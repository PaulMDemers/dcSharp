# Sonic Intro Milestone

Goal: use Sonic Adventure, Sonic Adventure 2, and Sonic Shuffle as the near-term retail canaries, with the first visible milestone being their intro sequences running far enough to produce sustained GD-ROM streaming and PVR TA/render output.

## Current Baseline

Run with:

```powershell
tools\probe-sonic.ps1 -AssertKnownFrontiers
```

The current baseline at a 50M instruction budget is:

| Title | Primary Local Media | Current Result | Frontier |
| --- | --- | --- | --- |
| Sonic Adventure | GDI | `FirmwareExit`, `PC=0x8C0000E8`, 46,680,428 instructions | The game performs 8 successful GD-ROM DMA reads, then reads the BIOS system vector at `0x8C0000E0`, gets `0x8C0000E8`, and jumps from `0x8C604350` with `r4=1`, matching the system menu/error function path. |
| Sonic Adventure 2 | CUE | `InstructionLimit`, `PC=0x8C135C10`, 50,000,000 instructions | The game still performs 45 successful GD-ROM reads and 2 TOC requests, but now moves past the former `0xA05F688C`/G2 DMA busy frontier. The active 50M frontier is inside the SA2 AICA/G2 PIO upload helper while initializing the sound driver. |
| Sonic Shuffle | GDI | `UnsupportedInstruction`, `PC=0x8C008300`, 7,952,388 instructions | The game now performs a successful GD-ROM read and reaches nonzero framebuffer bytes, then reads IP.BIN work-area bytes `0x8C0080FC/FE == 1` and jumps from `0x8C014C50` to boot-area address `0x8C008300`, which currently contains `0x0000`. |

The old shared `0x8C000000` callback frontier is retired for this trio: the firmware HLE now treats it as the default no-op callback and returns to `PR` with `r0=0`. Sonic Shuffle's former `PVR_SYNC_STATUS` wait is also retired by exposing bit `0x2000` during the synthetic VBlank status window. Sonic Adventure 2 and Sonic Shuffle additionally depend on GD-ROM commands using the parameter words captured at `SEND_COMMAND` time instead of rereading mutable stack parameter blocks when `GDROM_MAINLOOP` executes the queued command.

Latest Sonic Adventure 2 audio/G2 progress:

- G2 DMA channel 0 now models the AICA DMA registers at `0xA05F7800/04/08/0C/14/18`, copies between system RAM and AICA RAM, clears the start bit, and raises ASIC event `0x000f`.
- The former `0xA05F7818` busy-poll now reads clear, moving a 100M run into SA2's AICA/G2 PIO helpers instead of stopping at `0x8C153CEE`.
- Narrow SA2 fast-forwards now cover the exact VRAM clear loop, the exact `0x8C1004E0-0x8C1004F6` boot-work `"SEGA"` fill, the post-load `0x8C10052A` word clear and `0x8C100554` byte clear over system RAM, the AICA PIO read-word helper, the AICA status wait that depends on unimplemented AICA ARM-side command completion, the AICA PIO write loop used to upload sound-driver data, one-shot external/modem G2 read probes, the `0x8C1543A0` AICA word-read wrapper, the `0x8C16BF10` AICA byte-read adapter, the AICA work-queue no-work poll, the IRQ-side `0x8C153A90` AICA work-poll wrapper, the outer `0x8C12F7F2-0x8C12F802` AICA work-poll loop, the G2 DMA status clear/set helpers around `0x8C1709E0` and `0x8C170A98`, the follow-up `EXEC` completion wait that polls the AICA work-structure mailbox at `[*0x8C1833A4 + 0x80] + 0xD8`, the IP.BIN zero-bit glyph walker around `0x8C0089F6-0x8C008A74`, the IP.BIN glyph bit dispatch at `0x8C0089F6-0x8C008A12`, the IP.BIN dispatch-to-zero-tail aggregate at `0x8C0089F6-0x8C008A74`, the IP.BIN glyph-loop tail at `0x8C008A4C-0x8C008A74`, the IP.BIN glyph draw helper at `0x8C008AD0-0x8C008B08`, the set-bit glyph draw prefix at `0x8C008A14-0x8C008A4A`, the set-bit draw-to-loop-tail aggregate at `0x8C008A14-0x8C008A74`, the set-bit glyph draw tail at `0x8C008A3E-0x8C008A4C`, the IP.BIN signed quotient helper at `0x8C0098CC-0x8C00996A`, the IP.BIN signed remainder helper at `0x8C009A28-0x8C009ADA`, the empty pointer-table scan around `0x8C135CD8-0x8C135D06`, the trace-gated `0x8C10F440-0x8C10F446` cache-invalidate loop, the trace-gated `0x8C10F8D8-0x8C10F8E8` cache-writeback loop, the `0x8C134F3E-0x8C134F76` record-hash scan, the full SA2 byte-copy helper entry at `0x8C134CA8-0x8C134CBE`, the AICA no-work slot scan/cleanup pair plus mode-`0xFF` slot-entry sentinel/aggregate around `0x8C15B604-0x8C15B690` and `0x8C15B83C-0x8C15B860`, the AICA name-call bridge and loop tail around `0x8C15B91A-0x8C15B936`, the AICA channel setup bridge, callback-pointer tail, and mask-branch tail at `0x8C15C4DE-0x8C15C564`, the active channel setup call bridge at `0x8C15C566-0x8C15C724`, the active channel descriptor aggregate at `0x8C15C724-0x8C15C780`, the active channel helper prologue/tail around `0x8C15C724-0x8C15C780`, the active channel return bridge at `0x8C15C56E-0x8C15C57C`, the AICA post-setup return aggregate at `0x8C15C57C-0x8C15C5DA`, the AICA post-setup flag tail at `0x8C15C57E-0x8C15C5AA`, the AICA descriptor-copy helper at `0x8C15C622-0x8C15C680`, the inactive AICA channel tail at `0x8C15C780-0x8C15C856`, the AICA channel flag return tail at `0x8C15C5AC-0x8C15C5DA`, and the `0x8C023E20` PRS-style asset decompressor used after the 882,688-byte DMA read.
- The latest 120M no-trace SA2 profile reaches `PC=0x8C15AFA6` with 47/47 GD-ROM reads and no TA writes. CPU fast-forward has risen to `118,217,356`; the `0x8C10F440` cache loop, `0x8C134F3E` record-hash scan, `0x8C134CAA-0x8C134CBE` byte-copy body, `0x8C0089F8-0x8C008A12` IP.BIN bit-dispatch body, most repeated zero-tail redispatches, `0x8C008AD2-0x8C008B08` IP.BIN draw-helper body, `0x8C008A16-0x8C008A4A` set-bit draw-prefix body, `0x8C008A4E-0x8C008A74` glyph-loop tail body, `0x8C008A40-0x8C008A4A` set-bit draw-call tail, the `0x8C008A4C` loop-tail trigger, `0x8C0098CE-0x8C00996A` quotient-helper body, `0x8C009A2A-0x8C009ADA` remainder-helper body, the mode-`0xFF` `0x8C15B606-0x8C15B648` AICA slot-dispatch body, the `0x8C15C566-0x8C15C56C` active-channel call body, the `0x8C15C570` active-channel return delay slot, the `0x8C15C5AC` post-setup return trigger, `0x8C15C622`/`0x8C15C744` descriptor helper triggers, and the `0x8C15C726-0x8C15C77E` active-channel helper body are retired or reduced from the hot profile. The next profile-guided targets are remaining IP.BIN trigger PCs (`0x8C0089F6`, `0x8C008A14`) and the SA2 AICA/helper trigger clusters around `0x8C15B918`, `0x8C15C4DC`, `0x8C15C56E`, `0x8C15C57C`, `0x8C15C724`, and `0x8C15C780`, plus the newly prominent `0x8C16C3B8`/`0x8C16C65E` work-helper cluster.
- Work-entry status scan update: the exact `0x8C16C3B8-0x8C16C67E` AICA work-entry scan now fast-forwards the all-inactive 16-entry path while preserving the stack locals and final status word. The 120M SA2 profile still reaches `PC=0x8C15AFA6` with 47/47 GD-ROM reads and no TA writes, but CPU fast-forward rises to `118,345,053`; the `0x8C16C3B8`/`0x8C16C65E` cluster is gone from the top 40, exposing new smaller clusters around `0x8C167DFA`, `0x8C10F8E0`, `0x8C1004EE`, and `0x8C14D134`.
- Sparse word-clear update: the exact `0x8C167DFA-0x8C167E02` stride-24 system-RAM word clear now fast-forwards the remaining sparse stores after the first real `mov.w`. The 120M SA2 profile still reaches `PC=0x8C15AFA6` with 47/47 GD-ROM reads and no TA writes, CPU fast-forward rises to `118,361,436`, and the `0x8C167DFA` cluster is gone from the top 40. The next smaller clusters are around `0x8C10F8E0`, `0x8C1004EE`, `0x8C14D134`, `0x8C16C348`, and `0x8C15B212`.
- Cache writeback update: the exact `0x8C10F8D8-0x8C10F8E8` cache-line writeback loop now fast-forwards the remaining `ocbwb @r4`/`dt r5` walk after the first real cache opcode. The 120M SA2 profile still reaches `PC=0x8C15AFA6` with 47/47 GD-ROM reads and no TA writes, CPU fast-forward rises to `118,375,579`, and `0x8C10F8E0` is gone from the top 40. The next smaller clusters are around `0x8C1004EE`, `0x8C14D134`, `0x8C16C348`, `0x8C15B212`, and `0x8C10FF44`.
- Boot-work SEGA fill update: the exact `0x8C1004E0-0x8C1004F6` fill now writes the remaining `0x41474553` words from `0x8C00C000` through `0x8C00F3FC` after the first real store. The 120M SA2 profile still reaches `PC=0x8C15AFA6` with 47/47 GD-ROM reads and no TA writes, CPU fast-forward rises to `118,388,890`, and `0x8C1004EE-0x8C1004F4` is gone from the top 40. The next smaller clusters are around `0x8C14D134`, `0x8C16C348`, `0x8C15B212`, `0x8C10FF44`, and `0x8C16C1A4`.
- G2 DMA status-set caller update: the exact `0x8C15B212-0x8C15B21E` caller loop now aggregates the remaining calls to the `0x8C170A98` status-set helper after the table-base load, writing each observed channel status register and landing at the normal completion-flag epilogue. The 120M SA2 profile still reaches `PC=0x8C15AFA6` with 47/47 GD-ROM reads and no TA writes, CPU fast-forward rises to `118,511,001`, and the `0x8C15B212` loop body is gone from the top 40. The next smaller clusters include the IP.BIN glyph triggers at `0x8C0089F6`/`0x8C008A14`, AICA setup triggers around `0x8C15B918`, `0x8C15C4DC`, `0x8C15C57C`, and smaller spans around `0x8C10FF44` and `0x8C134DA2`.
- String-hash function-tail update: the exact `0x8C134D94-0x8C134DB2` uppercase/non-lowercase name-hash tail now fast-forwards from the literal-load point using the same byte guards as the loop helper. The 120M SA2 profile still reaches `PC=0x8C15AFA6` with 47/47 GD-ROM reads and no TA writes, CPU fast-forward rises to `118,519,706`, and the `0x8C134DA2-0x8C134DA6` hash-loop body drops out of the top 50. The surrounding parse/copy helper around `0x8C134DB4-0x8C134E48` remains exposed.
- Interrupt-source scan update: the exact `0x8C10FF2E-0x8C10FF4A` IRQ source bit-scan tail now fast-forwards only after a real callback has returned to `0x8C10FF42` and all remaining lower source bits are empty. The 120M SA2 profile still reaches `PC=0x8C15AFA6` with 47/47 GD-ROM reads and no TA writes, CPU fast-forward rises to `118,536,280`, and the `0x8C10FF2E/44` scan body drops out of the top 50.
- Record-name parser update: the exact `0x8C134DB4-0x8C134E48` record parser now fast-forwards the observed uppercase sector-table paths, including `"."`, `".."`, and trimmed uppercase filenames before hashing. The 120M SA2 profile now advances to `PC=0x8C16B59A` with 47/47 GD-ROM reads and no TA writes, CPU fast-forward rises to `118,640,827`, and the parser body drops out of the top 50; only the prologue trigger PCs remain visible before the aggregate fires.
- AICA active callback-tail update: the exact `0x8C16B598-0x8C16B5B6` active work-queue callback tail now fast-forwards the not-taken callback path when the active callback has no pending nested work, preserving the restored caller frame. A 120M SA2 profile still lands at the `0x8C16B59A` trigger because the instruction budget expires immediately after the branch, but CPU fast-forward rises to `118,648,555`; a 121M probe advances to `PC=0x8C170AAA` in the G2 DMA status-set helper with 47/47 GD-ROM reads and no TA writes.
- G2 DMA status-set helper tail update: the exact `0x8C170AA8-0x8C170AD6` tail now fast-forwards the post-interrupt-mask path when the profile lands partway through the helper, preserving parity with normal SH-4 stepping. The focused G2 status tests and the full 1060-test suite pass; a 122M SA2 probe now advances beyond the former `0x8C170AAA` set-helper stop to `PC=0x8C1709EA` in the sibling G2 DMA status-clear helper, with 47/47 GD-ROM reads and no TA writes.
- G2 DMA status-clear helper tail update: the exact `0x8C1709E8-0x8C1709F8` tail now fast-forwards the mid-helper channel-status clear path and returns to the normal caller loop with interpreter-parity register state. The focused G2 status tests and the full 1063-test suite pass; a 123M SA2 probe advances beyond the former `0x8C1709EA` clear-helper stop to `PC=0x8C1543A8` in the AICA word-read wrapper path, with 47/47 GD-ROM reads and no TA writes.
- AICA word-read wrapper tail update: the exact `0x8C1543A6-0x8C1543DC` tail now fast-forwards the early wrapper path before it reaches the existing G2 PIO read-helper shortcut, synthesizing the same temporary frame and AICA read result as the later wrapper fast-forward. The focused AICA wrapper tests and the full 1066-test suite pass; a 124M SA2 probe advances beyond the former `0x8C1543A8` wrapper stop to `PC=0x8C15B256`, just after the G2 DMA busy/status probe helper at `0x8C170BBC`, with 47/47 GD-ROM reads and no TA writes.
- G2 PIO read-word copy-loop update: the exact `0x8C1357F2-0x8C1357FE` 32-bit copy loop now has a guarded fast-forward with complete and partial-budget parity tests. The focused G2 PIO read-word tests and the full 1069-test suite pass. This is a local loop primitive for the AICA/G2 path; the 125001000-instruction SA2 probe still lands at `PC=0x8C15BEFA`, so the next high-value target remains the AICA descriptor/helper path around `0x8C15BED8-0x8C15C140`.
- AICA-register G2 PIO copy-loop update: the same `0x8C1357F2-0x8C1357FE` 32-bit copy-loop fast-forward now allows AICA register sources such as `0xA0710000`, matching the normal helper when SA2 copies AICA status/control words into stack temporaries. The focused G2 PIO read tests and full 1070-test suite pass; a 125004500-instruction SA2 probe still expires in the following `0x8C1356D8` helper prologue, but CPU fast-forward rises from `123,560,926` to `123,569,669` and the exposed two-word `0xA0710000` copy body is retired.
- G2 DMA status-set helper index-tail update: the exact `0x8C170AB4-0x8C170AD6` post-channel-index helper tail now fast-forwards when a trace budget lands after the second `shll2`, preserving interpreter-parity register and SR state. The focused G2 status-set tests and full 1073-test suite pass; a 125005300-instruction SA2 probe now jumps from the helper entry back to the enclosing caller loop for repeated status-set calls, writes the observed G2 status registers, and exposes the next frontier around the `0x8C15B212-0x8C15B21E` status-set caller loop, still with 47/47 GD-ROM reads and no TA writes.
- AICA empty work-table setup-tail update: the exact `0x8C16C6FA-0x8C16C838` setup-tail now fast-forwards the all-empty work-table scan when the budget lands after the initial literal load, preserving normal SH-4 stepping from `0x8C16C6FC`. The focused AICA work-table tests and full 1076-test suite pass; a 125007300-instruction SA2 probe moves through the empty table scan epilogue and exposes the sibling work-entry status scan tail at `PC=0x8C16C666`, still with 47/47 GD-ROM reads and no TA writes.
- AICA work-entry status-scan tail update: the exact `0x8C16C664-0x8C16C67E` mid-entry tail now completes the remaining all-inactive work-entry status scan after the current entry's accumulator update, preserving stack locals and interpreter parity. The focused work-entry status tests and full 1079-test suite pass; a 125007800-instruction SA2 probe advances into the next AICA/G2 busy-check wrapper around `0x8C153CE8-0x8C153D34`, still with 47/47 GD-ROM reads and no TA writes.
- AICA active-channel runner-gate update: the existing `0x8C15C724` active-channel aggregate now has a trace gate that includes its real trigger instruction instead of starting at `0x8C15C726`. Focused active-channel tests and the full 1079-test suite pass; a 125008700-instruction SA2 probe shows `0x8C15C724` collapsing to `0x8C15C780` and exposes the next channel-setup bridge budget edge at `0x8C15C56A`.
- AICA channel-setup active-bridge tail update: the exact `0x8C15C568-0x8C15C724` tail now completes the active-channel bridge after the saved descriptor pointer has already been pushed. The focused active-bridge tests and full 1081-test suite pass; a 125008850-instruction SA2 probe shows repeated active-channel bridge entries collapsing and exposes the broader channel setup path around `0x8C15C4FC`.
- AICA channel-setup entry-tail update: the exact `0x8C15C4FA-0x8C15C572/0x8C15C564` entry-tail now completes the broader channel setup helper from the already-built stack frame and first-entry pointer. The focused channel-setup tests and full 1083-test suite pass; descriptor copy/tail/return runner gates now include their true trigger instructions, and the next SA2 budget edge is the descriptor copy helper return path around `0x8C15C67C`.
- AICA channel-setup mask-tail update: the final `0x8C15C55C-0x8C15C562` mask load/test/branch now fast-forwards from late budget edges to either the zero-mask return path or active channel setup path. Focused mask-tail coverage compares the `0x8C15C55C`, `0x8C15C55E`, and `0x8C15C560` entry points against normal stepping.
- AICA channel-setup callback-tail update: the mid-entry `0x8C15C524-0x8C15C562` tail now completes callback-pointer lookup, slot mirror writes, pending-word promotion, and mask branching from the post-pointer-table-index state. Focused tests cover both pending and non-pending paths against normal stepping.
- A 500M post-PRS SA2 probe reaches later disc loading with 47 GD-ROM reads, including an 882,688-byte DMA read to `0x0C428800` and a 61,440-byte read to `0x0C300000`, but still has no TA writes. The old `0x8C12F56x`/`0x8C1543A0` `EXEC` wait collapses; `0xA08000F8` now contains `EXEC`; the old `0x8C023E34-0x8C023E88` decompressor cluster, `0x8C10052A-0x8C10055C` RAM-clear cluster, G2 DMA status set/clear helpers, IRQ-side `0x8C153A90` AICA work-poll body, and active `0x8C16B58E` AICA work-helper frontier are retired from the current higher-budget wall. CPU fast-forward is about 496.7M instructions. The current 500M frontier is `PC=0x8C15BF8C`, in a later SA2 sound/asset update path that still has no PVR TA traffic. The next work is to identify which remaining AICA work-structure fields are standing in for ARM/audio-driver completion so this becomes device behavior rather than another one-off skip.

## Work Plan

1. Keep `tools\probe-sonic.ps1 -AssertKnownFrontiers` as the quick regression loop while this milestone is active.
2. For Sonic Adventure 2, continue the AICA startup path: identify which AICA RAM status words are guest/ARM mailboxes, model the minimal command-completion semantics, then re-run higher budgets until either PVR TA traffic appears or a new renderer gate is exposed.
3. For Sonic Shuffle, trace why the boot work-area mode bytes at `0x8C0080FC/FE` remain `1` before the branch to `0x8C008300`; compare the path against BIOS handoff and soft-reset behavior before forcing any global boot byte.
4. For Sonic Adventure, trace the repeated sector `45166` reads and the state that leads to BIOS system function `1`; inspect the loaded read target, status buffers, and callback/table state around `0x8C60434C-0x8C604350`.
5. After each title passes its current frontier, use GD-ROM command logs, PVR TA logs, and framebuffer snapshots to chase the next intro-sequence checkpoint.

## Success Criteria

The milestone is not complete when the games merely reach a higher instruction count. It is complete when the Sonic probes show visible intro/title progress with at least one of:

- sustained successful GD-ROM reads after the current frontiers,
- nonzero PVR TA command traffic,
- a nonblank framebuffer or intro/title visual output.
