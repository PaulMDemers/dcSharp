# Sonic Intro Milestone

Goal: use Sonic Adventure, Sonic Adventure 2, and Sonic Shuffle as the near-term retail canaries, with the first visible milestone being their intro sequences running far enough to produce sustained GD-ROM streaming and PVR TA/render output.

## Current Baseline

Run with:

```powershell
tools\probe-sonic.ps1 -AssertKnownFrontiers
```

The current baseline at a 50M instruction budget is:

| Title | Primary Local Media | Current Result | Frontier |
| --- | --- | --- | --- |
| Sonic Adventure | GDI | `FirmwareExit`, `PC=0x8C0000E8`, 46,680,428 instructions | The game performs 8 successful GD-ROM DMA reads, then reads the BIOS system vector at `0x8C0000E0`, gets `0x8C0000E8`, and jumps from `0x8C604350` with `r4=1`, matching the system menu/error function path. |
| Sonic Adventure 2 | CUE | `InstructionLimit`, `PC=0x8C135C00`, 50,000,000 instructions | The game performs 45 successful GD-ROM reads and 2 TOC requests, moves past the former `0xA05F688C`/G2 DMA busy frontier and the later `0x8C15C7F4` AICA setup wall, then spends the remaining 50M budget in the SA2 AICA/G2 PIO upload helper cadence. |
| Sonic Shuffle | GDI | `UnsupportedInstruction`, `PC=0x8C008300`, 7,952,388 instructions | The game now performs a successful GD-ROM read and reaches nonzero framebuffer bytes, then reads IP.BIN work-area bytes `0x8C0080FC/FE == 1` and jumps from `0x8C014C50` to boot-area address `0x8C008300`, which currently contains `0x0000`. |

The old shared `0x8C000000` callback frontier is retired for this trio: the firmware HLE now treats it as the default no-op callback and returns to `PR` with `r0=0`. Sonic Shuffle's former `PVR_SYNC_STATUS` wait is also retired by exposing bit `0x2000` during the synthetic VBlank status window. Sonic Adventure 2 and Sonic Shuffle additionally depend on GD-ROM commands using the parameter words captured at `SEND_COMMAND` time instead of rereading mutable stack parameter blocks when `GDROM_MAINLOOP` executes the queued command.

Latest Sonic Adventure 2 audio/G2 progress:

- G2 DMA channel 0 now models the AICA DMA registers at `0xA05F7800/04/08/0C/14/18`, copies between system RAM and AICA RAM, clears the start bit, and raises ASIC event `0x000f`.
- The former `0xA05F7818` busy-poll now reads clear, moving a 100M run into SA2's AICA/G2 PIO helpers instead of stopping at `0x8C153CEE`.
- Narrow SA2 fast-forwards now cover the exact VRAM clear loop, the exact `0x8C1004E0-0x8C1004F6` boot-work `"SEGA"` fill, the post-load `0x8C10052A` word clear and `0x8C100554` byte clear over system RAM, the AICA PIO read-word helper, the AICA status wait that depends on unimplemented AICA ARM-side command completion, the AICA PIO write loop used to upload sound-driver data, one-shot external/modem G2 read probes, the `0x8C1543A0` AICA word-read wrapper including its call-tail, the `0x8C16BF10` AICA byte-read adapter, the AICA work-queue no-work poll, the IRQ-side `0x8C153A90` AICA work-poll wrapper, the outer `0x8C12F7F2-0x8C12F802` AICA work-poll loop, the G2 DMA status clear/set helpers around `0x8C1709E0` and `0x8C170A98`, the follow-up `EXEC` completion wait that polls the AICA work-structure mailbox at `[*0x8C1833A4 + 0x80] + 0xD8`, the IP.BIN zero-bit glyph walker around `0x8C0089F6-0x8C008A74`, the IP.BIN glyph bit dispatch at `0x8C0089F6-0x8C008A12`, the IP.BIN dispatch-to-zero-tail aggregate at `0x8C0089F6-0x8C008A74`, the IP.BIN glyph-loop tail at `0x8C008A4C-0x8C008A74`, the IP.BIN glyph draw helper at `0x8C008AD0-0x8C008B08`, the set-bit glyph draw prefix at `0x8C008A14-0x8C008A4A`, the set-bit draw-to-loop-tail aggregate at `0x8C008A14-0x8C008A74`, the set-bit glyph draw tail at `0x8C008A3E-0x8C008A4C`, the IP.BIN signed quotient helper at `0x8C0098CC-0x8C00996A`, the IP.BIN signed remainder helper at `0x8C009A28-0x8C009ADA`, the empty pointer-table scan around `0x8C135CD8-0x8C135D06`, the trace-gated `0x8C10F440-0x8C10F446` cache-invalidate loop, the trace-gated `0x8C10F8D8-0x8C10F8E8` cache-writeback loop, the `0x8C134F3E-0x8C134F76` record-hash scan, the full SA2 byte-copy helper entry at `0x8C134CA8-0x8C134CBE`, the AICA no-work slot scan/cleanup pair plus mode-`0xFF` slot-entry sentinel/aggregate and active mode dispatch bridge around `0x8C15B604-0x8C15B690` and `0x8C15B83C-0x8C15B860`, the AICA name-group descriptor-head aggregate around `0x8C15B8EC-0x8C15B918`, the AICA name-group tail around `0x8C15B938-0x8C15B940`, the AICA name-call bridge and loop tail plus next-call/active-setup bridge around `0x8C15B91A-0x8C15B936`, the AICA name-call-to-active-setup aggregate around `0x8C15B918-0x8C15C564`, the AICA channel setup bridge, callback-pointer tail, and mask-branch tail at `0x8C15C4DE-0x8C15C564`, the zero-mask descriptor-copy call bridge at `0x8C15C572-0x8C15C680`, the active channel setup call bridge at `0x8C15C566-0x8C15C724`, the active channel descriptor aggregate plus late descriptor mask tail at `0x8C15C724-0x8C15C780`, the active channel helper prologue/tail around `0x8C15C724-0x8C15C780`, the active channel return bridge at `0x8C15C56E-0x8C15C57C`, the AICA post-setup return aggregate at `0x8C15C57C-0x8C15C5DA`, the AICA post-setup flag tail at `0x8C15C57E-0x8C15C5AA`, the AICA descriptor-copy helper at `0x8C15C622-0x8C15C680`, the inactive AICA channel tail at `0x8C15C780-0x8C15C856`, the AICA channel flag return tail at `0x8C15C5AC-0x8C15C5DA`, and the `0x8C023E20` PRS-style asset decompressor used after the 882,688-byte DMA read.
- The latest 120M no-trace SA2 profile reaches `PC=0x8C15AFA6` with 47/47 GD-ROM reads and no TA writes. CPU fast-forward has risen to `118,217,356`; the `0x8C10F440` cache loop, `0x8C134F3E` record-hash scan, `0x8C134CAA-0x8C134CBE` byte-copy body, `0x8C0089F8-0x8C008A12` IP.BIN bit-dispatch body, most repeated zero-tail redispatches, `0x8C008AD2-0x8C008B08` IP.BIN draw-helper body, `0x8C008A16-0x8C008A4A` set-bit draw-prefix body, `0x8C008A4E-0x8C008A74` glyph-loop tail body, `0x8C008A40-0x8C008A4A` set-bit draw-call tail, the `0x8C008A4C` loop-tail trigger, `0x8C0098CE-0x8C00996A` quotient-helper body, `0x8C009A2A-0x8C009ADA` remainder-helper body, the mode-`0xFF` `0x8C15B606-0x8C15B648` AICA slot-dispatch body, the `0x8C15C566-0x8C15C56C` active-channel call body, the `0x8C15C570` active-channel return delay slot, the `0x8C15C5AC` post-setup return trigger, `0x8C15C622`/`0x8C15C744` descriptor helper triggers, and the `0x8C15C726-0x8C15C77E` active-channel helper body are retired or reduced from the hot profile. The next profile-guided targets are remaining IP.BIN trigger PCs (`0x8C0089F6`, `0x8C008A14`) and the SA2 AICA/helper trigger clusters around `0x8C15B918`, `0x8C15C4DC`, `0x8C15C56E`, `0x8C15C57C`, `0x8C15C724`, and `0x8C15C780`, plus the newly prominent `0x8C16C3B8`/`0x8C16C65E` work-helper cluster.
- Work-entry status scan update: the exact `0x8C16C3B8-0x8C16C67E` AICA work-entry scan now fast-forwards the all-inactive 16-entry path while preserving the stack locals and final status word. The 120M SA2 profile still reaches `PC=0x8C15AFA6` with 47/47 GD-ROM reads and no TA writes, but CPU fast-forward rises to `118,345,053`; the `0x8C16C3B8`/`0x8C16C65E` cluster is gone from the top 40, exposing new smaller clusters around `0x8C167DFA`, `0x8C10F8E0`, `0x8C1004EE`, and `0x8C14D134`.
- Sparse word-clear update: the exact `0x8C167DFA-0x8C167E02` stride-24 system-RAM word clear now fast-forwards the remaining sparse stores after the first real `mov.w`. The 120M SA2 profile still reaches `PC=0x8C15AFA6` with 47/47 GD-ROM reads and no TA writes, CPU fast-forward rises to `118,361,436`, and the `0x8C167DFA` cluster is gone from the top 40. The next smaller clusters are around `0x8C10F8E0`, `0x8C1004EE`, `0x8C14D134`, `0x8C16C348`, and `0x8C15B212`.
- Cache writeback update: the exact `0x8C10F8D8-0x8C10F8E8` cache-line writeback loop now fast-forwards the remaining `ocbwb @r4`/`dt r5` walk after the first real cache opcode. The 120M SA2 profile still reaches `PC=0x8C15AFA6` with 47/47 GD-ROM reads and no TA writes, CPU fast-forward rises to `118,375,579`, and `0x8C10F8E0` is gone from the top 40. The next smaller clusters are around `0x8C1004EE`, `0x8C14D134`, `0x8C16C348`, `0x8C15B212`, and `0x8C10FF44`.
- Boot-work SEGA fill update: the exact `0x8C1004E0-0x8C1004F6` fill now writes the remaining `0x41474553` words from `0x8C00C000` through `0x8C00F3FC` after the first real store. The 120M SA2 profile still reaches `PC=0x8C15AFA6` with 47/47 GD-ROM reads and no TA writes, CPU fast-forward rises to `118,388,890`, and `0x8C1004EE-0x8C1004F4` is gone from the top 40. The next smaller clusters are around `0x8C14D134`, `0x8C16C348`, `0x8C15B212`, `0x8C10FF44`, and `0x8C16C1A4`.
- G2 DMA status-set caller update: the exact `0x8C15B212-0x8C15B21E` caller loop now aggregates the remaining calls to the `0x8C170A98` status-set helper after the table-base load, writing each observed channel status register and landing at the normal completion-flag epilogue. The 120M SA2 profile still reaches `PC=0x8C15AFA6` with 47/47 GD-ROM reads and no TA writes, CPU fast-forward rises to `118,511,001`, and the `0x8C15B212` loop body is gone from the top 40. The next smaller clusters include the IP.BIN glyph triggers at `0x8C0089F6`/`0x8C008A14`, AICA setup triggers around `0x8C15B918`, `0x8C15C4DC`, `0x8C15C57C`, and smaller spans around `0x8C10FF44` and `0x8C134DA2`.
- String-hash function-tail update: the exact `0x8C134D94-0x8C134DB2` uppercase/non-lowercase name-hash tail now fast-forwards from the literal-load point using the same byte guards as the loop helper. The 120M SA2 profile still reaches `PC=0x8C15AFA6` with 47/47 GD-ROM reads and no TA writes, CPU fast-forward rises to `118,519,706`, and the `0x8C134DA2-0x8C134DA6` hash-loop body drops out of the top 50. The surrounding parse/copy helper around `0x8C134DB4-0x8C134E48` remains exposed.
- Interrupt-source scan update: the exact `0x8C10FF2E-0x8C10FF4A` IRQ source bit-scan tail now fast-forwards only after a real callback has returned to `0x8C10FF42` and all remaining lower source bits are empty. The 120M SA2 profile still reaches `PC=0x8C15AFA6` with 47/47 GD-ROM reads and no TA writes, CPU fast-forward rises to `118,536,280`, and the `0x8C10FF2E/44` scan body drops out of the top 50.
- IP.BIN pattern-fill entry update: the `0x8C008EE4-0x8C008F4E` framebuffer pattern fill now fast-forwards from the loop entry instead of waiting for the first branch backedge. The 50M SA2 probe reaches `PC=0x8C135C00`, keeps 45 successful GD-ROM reads plus 2 TOC requests, raises CPU fast-forward to `49,894,245`, and removes the `0x8C008EE4-0x8C008F4E` body from the top 80 profile. The remaining fixed-budget pressure is led by IP.BIN glyph dispatch/byte-exit triggers and the SA2 AICA/G2 PIO upload cadence.
- G2 PIO scalar-path update: the `0x8C135BF4-0x8C135C16` scalar AICA RAM upload path now fast-forwards the current word write plus source/destination/index tail, then chains into the existing bulk upload shortcut when the loop continues. The exact 50M SA2 probe still expires at `PC=0x8C135C00` because the instruction budget lands before this tail has enough room to fire, but slightly longer and later probes no longer have to walk this scalar path one instruction at a time.
- Record-name parser update: the exact `0x8C134DB4-0x8C134E48` record parser now fast-forwards the observed uppercase sector-table paths, including `"."`, `".."`, and trimmed uppercase filenames before hashing. The 120M SA2 profile now advances to `PC=0x8C16B59A` with 47/47 GD-ROM reads and no TA writes, CPU fast-forward rises to `118,640,827`, and the parser body drops out of the top 50; only the prologue trigger PCs remain visible before the aggregate fires.
- AICA active callback-tail update: the exact `0x8C16B598-0x8C16B5B6` active work-queue callback tail now fast-forwards the not-taken callback path when the active callback has no pending nested work, preserving the restored caller frame. A 120M SA2 profile still lands at the `0x8C16B59A` trigger because the instruction budget expires immediately after the branch, but CPU fast-forward rises to `118,648,555`; a 121M probe advances to `PC=0x8C170AAA` in the G2 DMA status-set helper with 47/47 GD-ROM reads and no TA writes.
- AICA active callback literal-tail update: the exact post-branch literal tail at `0x8C16B59C-0x8C16B5B6` now has its own guarded backstop when a probe lands after loading the callback target and active-work helper literal. The 120M SA2 probe still stops at `PC=0x8C16B59E` because the instruction budget is exhausted before any remaining tail can legally be skipped, while a 121M probe reaches the existing G2 DMA status-set path at `PC=0x8C170AAE` with 47/47 GD-ROM reads and no TA writes.
- G2 DMA status-set helper tail update: the exact `0x8C170AA8-0x8C170AD6` tail now fast-forwards the post-interrupt-mask path when the profile lands partway through the helper, preserving parity with normal SH-4 stepping. The focused G2 status tests and the full 1060-test suite pass; a 122M SA2 probe now advances beyond the former `0x8C170AAA` set-helper stop to `PC=0x8C1709EA` in the sibling G2 DMA status-clear helper, with 47/47 GD-ROM reads and no TA writes.
- G2 DMA status-clear helper tail update: the exact `0x8C1709E8-0x8C1709F8` tail now fast-forwards the mid-helper channel-status clear path and returns to the normal caller loop with interpreter-parity register state. The focused G2 status tests and the full 1063-test suite pass; a 123M SA2 probe advances beyond the former `0x8C1709EA` clear-helper stop to `PC=0x8C1543A8` in the AICA word-read wrapper path, with 47/47 GD-ROM reads and no TA writes.
- AICA word-read wrapper tail update: the exact `0x8C1543A6-0x8C1543DC` tail now fast-forwards the early wrapper path before it reaches the existing G2 PIO read-helper shortcut, synthesizing the same temporary frame and AICA read result as the later wrapper fast-forward. The focused AICA wrapper tests and the full 1066-test suite pass; a 124M SA2 probe advances beyond the former `0x8C1543A8` wrapper stop to `PC=0x8C15B256`, just after the G2 DMA busy/status probe helper at `0x8C170BBC`, with 47/47 GD-ROM reads and no TA writes.
- G2 PIO read-word post-status tail update: the exact `0x8C13575E-0x8C135838` path now fast-forwards the pending AICA word copy, observed G2 status clear, helper counter update, and saved-frame restore after the first busy/status probe. Focused parity/short-budget tests pass; the exact 500000200 SA2 cutoff still stops at `PC=0x8C13575E`, while a 500000420 probe reaches `PC=0x8C16B58E` in the active AICA callback path with 47/47 GD-ROM reads and no TA writes.
- G2 PIO read-word copy-loop update: the exact `0x8C1357F2-0x8C1357FE` 32-bit copy loop now has a guarded fast-forward with complete and partial-budget parity tests. The focused G2 PIO read-word tests and the full 1069-test suite pass. This is a local loop primitive for the AICA/G2 path; the 125001000-instruction SA2 probe still lands at `PC=0x8C15BEFA`, so the next high-value target remains the AICA descriptor/helper path around `0x8C15BED8-0x8C15C140`.
- AICA-register G2 PIO copy-loop update: the same `0x8C1357F2-0x8C1357FE` 32-bit copy-loop fast-forward now allows AICA register sources such as `0xA0710000`, matching the normal helper when SA2 copies AICA status/control words into stack temporaries. The focused G2 PIO read tests and full 1070-test suite pass; a 125004500-instruction SA2 probe still expires in the following `0x8C1356D8` helper prologue, but CPU fast-forward rises from `123,560,926` to `123,569,669` and the exposed two-word `0xA0710000` copy body is retired.
- G2 DMA status-set helper index-tail update: the exact `0x8C170AB4-0x8C170AD6` post-channel-index helper tail now fast-forwards when a trace budget lands after the second `shll2`, preserving interpreter-parity register and SR state. The focused G2 status-set tests and full 1073-test suite pass; a 125005300-instruction SA2 probe now jumps from the helper entry back to the enclosing caller loop for repeated status-set calls, writes the observed G2 status registers, and exposes the next frontier around the `0x8C15B212-0x8C15B21E` status-set caller loop, still with 47/47 GD-ROM reads and no TA writes.
- AICA empty work-table setup-tail update: the exact `0x8C16C6FA-0x8C16C838` setup-tail now fast-forwards the all-empty work-table scan when the budget lands after the initial literal load, preserving normal SH-4 stepping from `0x8C16C6FC`. The focused AICA work-table tests and full 1076-test suite pass; a 125007300-instruction SA2 probe moves through the empty table scan epilogue and exposes the sibling work-entry status scan tail at `PC=0x8C16C666`, still with 47/47 GD-ROM reads and no TA writes.
- AICA work-entry status-scan tail update: the exact `0x8C16C664-0x8C16C67E` mid-entry tail now completes the remaining all-inactive work-entry status scan after the current entry's accumulator update, preserving stack locals and interpreter parity. The focused work-entry status tests and full 1079-test suite pass; a 125007800-instruction SA2 probe advances into the next AICA/G2 busy-check wrapper around `0x8C153CE8-0x8C153D34`, still with 47/47 GD-ROM reads and no TA writes.
- AICA active-channel runner-gate update: the existing `0x8C15C724` active-channel aggregate now has a trace gate that includes its real trigger instruction instead of starting at `0x8C15C726`. Focused active-channel tests and the full 1079-test suite pass; a 125008700-instruction SA2 probe shows `0x8C15C724` collapsing to `0x8C15C780` and exposes the next channel-setup bridge budget edge at `0x8C15C56A`.
- AICA channel-setup active-bridge tail update: the exact `0x8C15C568-0x8C15C724` tail now completes the active-channel bridge after the saved descriptor pointer has already been pushed. The focused active-bridge tests and full 1081-test suite pass; a 125008850-instruction SA2 probe shows repeated active-channel bridge entries collapsing and exposes the broader channel setup path around `0x8C15C4FC`.
- AICA channel-setup entry-tail update: the exact `0x8C15C4FA-0x8C15C572/0x8C15C564` entry-tail now completes the broader channel setup helper from the already-built stack frame and first-entry pointer. The focused channel-setup tests and full 1083-test suite pass; descriptor copy/tail/return runner gates now include their true trigger instructions, and the next SA2 budget edge is the descriptor copy helper return path around `0x8C15C67C`.
- AICA channel-setup mask-tail update: the final `0x8C15C55C-0x8C15C562` mask load/test/branch now fast-forwards from late budget edges to either the zero-mask return path or active channel setup path. Focused mask-tail coverage compares the `0x8C15C55C`, `0x8C15C55E`, and `0x8C15C560` entry points against normal stepping.
- AICA channel-setup callback-tail update: the mid-entry `0x8C15C524-0x8C15C562` tail now completes callback-pointer lookup, slot mirror writes, pending-word promotion, and mask branching from the post-pointer-table-index state. Focused tests cover both pending and non-pending paths against normal stepping.
- AICA active descriptor mask-tail update: the late `0x8C15C768-0x8C15C780` path now completes the final work-byte mask checks from budget edges that cannot afford the full descriptor tail. Focused descriptor tests compare the five skipped instructions against normal stepping.
- AICA read-wrapper call-tail update: the late `0x8C1543C6-0x8C1543D0` call setup now bridges into the already-modeled G2 PIO read-word helper and epilogue from tight budget edges. Focused tests compare it against normal stepping plus the existing full wrapper fast-forward.
- AICA mirror-copy loop update: the per-frame `0x8C135DDC-0x8C135E1C` 24-entry mirror copy now fast-forwards the remaining source/destination entries from the taken branch tail, preserving normal stepping and the scaled SH-4 longword displacements. Focused tests cover parity and short-budget refusal; the full suite passes at 1096 tests. A 300M SA2 probe still lands in the AICA/G2 frame service path at `PC=0x8C15B266` with 47/47 GD-ROM reads and no TA writes, but the `0x8C135DE8-0x8C135E16` copy body is gone from the hot profile.
- Interrupt callback status inactive-tail update: the exact `0x8C14C9BA-0x8C14CB80` four-entry status scan now fast-forwards from the `0x8C14CB7A` not-taken loop edge when every remaining callback-table entry is inactive, preserving the stack-local count/offset bookkeeping and refusing active entries. Focused parity/guard tests and the full 1099-test suite pass; a 300M SA2 probe still lands at `PC=0x8C15B266`, but CPU fast-forward rises to `295,518,819` and the `0x8C14C9BA/0x8C14CB5C/0x8C14CB74` body drops out of the hot profile.
- Interrupt FPU wrapper saved-register tail update: the exact `0x8C14D212-0x8C14D22A` tail now fast-forwards after the wrapper has already saved FPSCR and `fr0-fr6`, preserving the remaining `fr7-fr15` stack image and callback dispatch state. Focused parity and short-budget tests pass; a 500000800 SA2 probe advances past the former `PC=0x8C14D212` stop to `PC=0x8C15B5B6`, with 47/47 GD-ROM reads and no TA writes.
- AICA pointer-mask helper update: the tiny `0x8C15CE86-0x8C15CE8A` helper now fast-forwards the `rts` delay-slot `r0 = r4 & 0x0FFFFFFF` tail after the literal mask load, preserving normal SH-4 return semantics for both descriptor and IRQ-side callers. Focused parity/guard tests and the full 1101-test suite pass; a 300M SA2 probe still lands at `PC=0x8C15B266`, but CPU fast-forward rises to `295,528,283` and `0x8C15CE88/0x8C15CE8A` drop out of the hot profile. The remaining exposed work is the caller/descriptor body around `0x8C15B8EC` and `0x8C15BED8-0x8C15C140`.
- AICA descriptor-update prologue update: the `0x8C15BED8-0x8C15BF1C` prologue now fast-forwards the observed inactive-slot path after the first register push, preserving the saved frame, local argument bytes, `channel * 44` slot offset, and `slot+6 == 0` branch state. Focused parity/guard tests and the full 1104-test suite pass; a 300M SA2 probe still lands at `PC=0x8C15B266`, but CPU fast-forward rises to `295,620,557` and the prologue body from `0x8C15BEDA` through `0x8C15BF0C` drops out of the hot profile. The next exposed descriptor span begins at `0x8C15BF1C`.
- AICA descriptor setup update: the `0x8C15BF1C-0x8C15BF72` setup path now fast-forwards the branch-taken `workBase+0xFD8 <= 0` case, marking the slot active and preserving the descriptor byte/bitfield stack locals before landing at the pointer-advance path. Focused parity/guard tests and the full 1107-test suite pass; a 300M SA2 probe still lands at `PC=0x8C15B266`, CPU fast-forward rises to `295,656,047`, and `0x8C15BF1E-0x8C15BF58` drops out of the hot profile. The next exposed descriptor span begins at `0x8C15BF72`.
- AICA descriptor first-word update: the `0x8C15BF72-0x8C15BFC2` pointer-advance and first descriptor-word path now fast-forwards through the `0x8C15CE86` pointer-mask helper, preserving the descriptor pointer update, stack locals, first descriptor write, `PR`, and SH-4 delay-slot return state. Focused parity/guard tests and the full 1110-test suite pass; a 300M SA2 probe still lands at `PC=0x8C15B266`, CPU fast-forward rises to `295,798,007`, and `0x8C15BF74-0x8C15BFC0` plus `0x8C15CE86-0x8C15CE8A` drop out of the hot profile. The next exposed descriptor span begins at `0x8C15BFC2` and branches through `0x8C15C038`.
- AICA descriptor second/third-word update: the branch-taken `0x8C15BFC2-0x8C15C08C` path now fast-forwards the second descriptor pointer write and packed third descriptor word when `workBase+0x10C4 != 1` and no extra descriptor words remain (`r11 <= 0`). Focused parity/guard tests and the full 1114-test suite pass; a 300M SA2 probe still lands at `PC=0x8C15B266`, CPU fast-forward rises to `295,876,118`, and `0x8C15BFC4-0x8C15BFE2` plus `0x8C15C038-0x8C15C058` drop out of the hot profile. The next exposed descriptor span begins at `0x8C15C08C`.
- AICA descriptor counter-return update: the common `0x8C15C08C` descriptor epilogue now fast-forwards the branch-taken counter update and restored-register return path when `workBase+0x10C4 != 1`, `r12 <= 0`, and the masked descriptor counter remains nonzero. Focused parity/guard tests and the full 1119-test suite pass; a 300M SA2 probe still lands at `PC=0x8C15B266`, CPU fast-forward rises to `296,060,666`, and the `0x8C15C08E-0x8C15C140` body drops out of the hot profile. The next exposed descriptor caller span begins around `0x8C15C416`.
- AICA descriptor head-update update: the common `0x8C15C416-0x8C15C4C4` caller helper now fast-forwards the non-sentinel descriptor-head path, updating the slot byte transition flags and returning to the name/setup caller when `workBase+0x10C4 != 1`. Focused parity/guard tests and the full 1123-test suite pass; a 300M SA2 probe still lands at `PC=0x8C15B266`, CPU fast-forward rises to `296,305,547`, and `0x8C15C418-0x8C15C4C4` drops out of the hot profile. The next exposed repeated spans are around `0x8C15C572` and the descriptor-copy/return path.
- AICA active-channel post-setup return update: the `0x8C15C56E-0x8C15C5DA` aggregate now bridges the active-channel return `bra` delay slot, the `0x8C15C57C` post-setup entry, and the common flag-return epilogue in one validated path for both callback and non-callback slots. Focused parity/guard tests and the full 1127-test suite pass; a 300M SA2 probe still lands at `PC=0x8C15B266`, CPU fast-forward rises to `296,323,292`, and `0x8C15C57C` drops out of the top profile. The remaining AICA trigger pressure is around `0x8C15B918`, `0x8C15B92E`, `0x8C15C4DC`, `0x8C15C564`, `0x8C15C56E`, `0x8C15C724`, and `0x8C15C780`.
- AICA active descriptor-return update: the `0x8C15C564-0x8C15C56E` aggregate now bridges active channel setup through the descriptor helper and inactive-channel return, preserving the descriptor/local stack frame and slot reset writes before landing at the post-setup return bridge. Focused parity/guard tests and the full 1130-test suite pass; a 300M SA2 probe still lands at `PC=0x8C15B266`, CPU fast-forward rises to `296,358,782`, and `0x8C15C724`/`0x8C15C780` drop out of the top profile. The remaining AICA trigger pressure is concentrated around `0x8C15B918`, `0x8C15B92E`, `0x8C15C4DC`, `0x8C15C564`, and `0x8C15C56E`.
- AICA name-call active-setup update: the `0x8C15B918-0x8C15C564` aggregate now bridges the outer name-call helper into the active channel-setup mask path, preserving the nested stack frames, pending-word promotion, slot mirror bytes, callback pointer lookup, and active mask state before landing on the existing active descriptor-return shortcut. Focused parity/guard tests and the full 1134-test suite pass; a 300M SA2 probe still lands at `PC=0x8C15B266`, CPU fast-forward rises to `296,376,527`, and `0x8C15C4DC` drops out of the top 40 profile. The remaining AICA trigger pressure is concentrated around `0x8C15B918`, `0x8C15B92E`, `0x8C15C564`, and `0x8C15C56E`.
- AICA name-group descriptor-head update: the `0x8C15B8EC-0x8C15B918` aggregate now bridges the outer group prelude through the descriptor-head helper and post-return index setup when the group name byte and descriptor word are active. Focused parity/guard tests and the full 1138-test suite pass; a 300M SA2 probe still lands at `PC=0x8C15B266`, CPU fast-forward rises to `296,454,605`, and `0x8C15B8EE-0x8C15B916` drops out of the hot profile. The remaining AICA pressure is mostly trigger PCs plus the zero-mask/post-setup path around `0x8C15C572-0x8C15C5DE`.
- AICA name-group tail update: the tiny `0x8C15B938-0x8C15B940` outer group tail now fast-forwards the four-group scan branch after the real increment at `0x8C15B938`. Focused parity/guard tests and the full 1141-test suite pass; a 300M SA2 probe still lands at `PC=0x8C15B266`, CPU fast-forward rises to `296,468,801`, and `0x8C15B93A/0x8C15B93C/0x8C15B93E` drop out of the hot profile.
- AICA zero-mask descriptor-copy call update: the `0x8C15C572-0x8C15C680` bridge now fast-forwards the zero-mask path's call setup into the descriptor-copy helper tail, preserving `r4-r7`, `PR=0x8C15C57C`, and the stack argument load. Focused parity/guard tests and the full 1143-test suite pass; a 300M SA2 probe still lands at `PC=0x8C15B266`, CPU fast-forward rises to `296,482,997`, and `0x8C15C574/0x8C15C576/0x8C15C578/0x8C15C57A` drop out of the hot profile, exposing the `0x8C15C680+` helper body.
- AICA zero-mask descriptor-copy helper prologue update: the `0x8C15C680-0x8C15C694` helper prologue now fast-forwards the register saves and descriptor-copy `bsr` delay slot, preserving the nested stack frame and landing at the existing `0x8C15C622` descriptor-copy helper. Focused parity/guard tests and the full 1145-test suite pass; a 300M SA2 probe still lands at `PC=0x8C15B266`, CPU fast-forward rises to `296,514,938`, and `0x8C15C682-0x8C15C692` drops out of the hot profile, exposing the post-copy path at `0x8C15C694+`.
- AICA zero-mask status-update helper update: the `0x8C15C694-0x8C15C69A` call bridge and `0x8C15C5DA-0x8C15C620` status helper now fast-forward the observed slot control modes while refusing the unmodeled `slot+8 == 0/0xFF` branch. Focused parity/guard tests and the full 1153-test suite pass; a 300M SA2 probe still lands at `PC=0x8C15B266`, CPU fast-forward rises to `296,572,901`, and the `0x8C15C696/0x8C15C698` call body plus `0x8C15C5DC-0x8C15C620` helper body drop out of the hot profile, exposing the `0x8C15C69A-0x8C15C712` slot flag/update epilogue.
- AICA zero-mask no-event epilogue update: the guarded `0x8C15C69A-0x8C15C712` epilogue now fast-forwards the no-rising/no-falling-event path, restoring the saved `PR/r12-r14` frame and refusing mode `1` or any slot flag transition that would call the `0x8C15C856` event helper. Focused parity/guard tests and the full 1159-test suite pass; a 300M SA2 probe still lands at `PC=0x8C15B266`, CPU fast-forward rises to `296,705,359`, and `0x8C15C69C-0x8C15C712` drops out of the hot profile.
- A 500M post-PRS SA2 probe reaches later disc loading with 47 GD-ROM reads, including an 882,688-byte DMA read to `0x0C428800` and a 61,440-byte read to `0x0C300000`, but still has no TA writes. The old `0x8C12F56x`/`0x8C1543A0` `EXEC` wait collapses; `0xA08000F8` now contains `EXEC`; the old `0x8C023E34-0x8C023E88` decompressor cluster, `0x8C10052A-0x8C10055C` RAM-clear cluster, G2 DMA status set/clear helpers, IRQ-side `0x8C153A90` AICA work-poll body, and active `0x8C16B58E` AICA work-helper frontier are retired from the current higher-budget wall. CPU fast-forward is about 496.7M instructions. The current 500M frontier is `PC=0x8C15BF8C`, in a later SA2 sound/asset update path that still has no PVR TA traffic. The next work is to identify which remaining AICA work-structure fields are standing in for ARM/audio-driver completion so this becomes device behavior rather than another one-off skip.
- SR callback-list exit-tail update: the exact `0x8C10FE8E-0x8C10FE96` dispatch epilogue now fast-forwards only when the callback-list cursor is null after the final real callback returns, restoring saved `PR/r14` from the stack and landing at the wrapper return target. Focused parity/guard tests and the full 1210-test suite pass. The 50M SA2 probe `sa2-sr-callback-list-exit-50m-20260612-162232` stays at `PC=0x8C15C7F4` with no GD-ROM reads or TA writes; CPU fast-forward rises to `49,132,328`, and `0x8C10FE90` drops from `1,839` to `1,226` hits. The two callback bodies are still real execution and remain the better target for AICA ARM/mailbox modeling.
- SR callback-list next-active update: the exact `0x8C10FE8C-0x8C10FE90` active-node tail now fast-forwards the `tst/bf` pair after a callback returns and the next list node is non-null, landing at the next callback dispatch without modeling either callback body. Focused parity/guard tests and the full 1214-test suite pass. The 50M SA2 probe `sa2-sr-callback-list-next-active-50m-20260612-163441` stays at `PC=0x8C15C7F4`, still with no GD-ROM reads or TA writes; CPU fast-forward rises to `49,133,554`, `0x8C10FE8E` drops from `1,839` to `1,226`, and `0x8C10FE90` leaves the top profile slice.
- AICA inactive callback helper update: the exact `0x8C135D40-0x8C135DB0` helper now fast-forwards the proven-inactive return path after `0x8C29BEAC == 0`, restoring saved `PR/r13/r14` from the stack and returning to the caller. Focused parity/guard tests and the full 1218-test suite pass. The 50M SA2 probe `sa2-aica-inactive-callback-helper-50m-20260612-164045` stays at `PC=0x8C15C7F4` with no GD-ROM reads or TA writes; CPU fast-forward rises to `49,136,614`. The top profile remains dominated by AICA name/descriptor triggers, so the next higher-value work is still the callback bodies and ARM/mailbox completion model.
- AICA mailbox activity instrumentation update: audio snapshots, run summaries, CLI boot-smoke output, and desktop summaries now report serviced AICA mailbox packets with command id, timestamp, size, tail movement, command name, and result. Focused mailbox tests and the full 1219-test suite pass. The 50M SA2 probe `sa2-aica-mailbox-summary-50m-20260612-193945` stays at `PC=0x8C15C7F4` with `mailbox=0`, confirming this frontier is not using the fixed KOS-style queue offsets and should be investigated through SA2's work-structure/callback fields next.
- AICA command-queue discovery update: audio snapshots, run summaries, CLI output, and desktop summaries now include plausible AICA command-queue headers discovered anywhere in AICA RAM. Focused queue-discovery tests and the full 1222-test suite pass. The 50M SA2 probe `sa2-aica-queue-discovery-50m-20260612-194455` stays at `PC=0x8C15C7F4` with `mailbox=0` and `queues=0`, so the current SA2 path is not waiting on a KOS-shaped queue at a hidden offset; the next trace should stay on SA2's work-structure/callback fields and any uploaded AICA driver markers.
- AICA RAM text-marker update: audio snapshots, run summaries, CLI output, and desktop summaries now report printable AICA RAM marker strings, capped to useful previews. Focused marker tests and the full 1224-test suite pass. The 50M SA2 probe `sa2-aica-text-markers-50m-20260612-194931` stays at `PC=0x8C15C7F4` with `mailbox=0`, `queues=0`, and `text=0`; no visible AICA RAM driver marker is present at this frontier, so the immediate target remains SH-4-side work/callback fields.
- Maple boot-smoke visibility update: `media boot-smoke` and `tools/probe-long.ps1` now print Maple transfer/DMA summaries. The 300M SA2 probe `sa2-maple-aica-frontier-300m-20260612-200413` still lands at `PC=0x8C15BF8E` with no GD-ROM reads or TA writes and AICA `mailbox=0/queues=0/text=0`, but now shows `78,267` Maple transfers, `26,089` Maple DMA batches, and repeated A0 `GetCondition` plus absent expansion-device `DeviceInfo` probes. The live `0x8C2A23FC` work arena contains Maple/controller identity fields (`PORT`, `DEV`, `INFO`, `ENBL`, `STAT`, `RADR`, `TOUT`, `DRQC`, `FUNC`, `END`), so this budget wall is currently dominated by Maple/device-service work intertwined with the descriptor helper path, not by an observed AICA ARM mailbox.
- AICA no-work slot loop-tail update: the exact `0x8C15B686-0x8C15B68E` slot-advance tail now fast-forwards after the real `add #120,r11`, preserving the next-entry and scan-exit branch states. Focused no-work-slot tests and the full 1227-test suite pass. The 300M SA2 probe `sa2-nowork-tail-300m-20260612-214042` still lands at `PC=0x8C15BF8E` with no GD-ROM reads or TA writes, Maple remains at `78,267` transfers / `26,089` DMA batches, and `0x8C15B688/B68A/B68C/B68E` drop out of the top profile slice. The newly exposed pressure is the `0x8C15B604` slot-entry trigger, so the next high-value fast-forward is an aggregate that bridges this tail directly into the existing no-work slot-entry/scan helpers when the entry is inactive.
- AICA no-work slot tail aggregate update: the loop-tail helper now chains directly into the existing inactive slot scan when the next entries require no active mode dispatch. Focused no-work-slot tests and the full 1229-test suite pass. The 300M SA2 probe `sa2-nowork-tail-aggregate-300m-20260612-214652` still lands at `PC=0x8C15BF8E`, but CPU fast-forward rises to `267,553,113`, batches drop to `1,493,139`, and `0x8C15B604` falls from `626,113` hits to `78,265`. The remaining top pressure is the name/setup trigger cluster around `0x8C15B92E`, plus per-frame callback and descriptor-helper trigger PCs.
- Interrupt callback empty-scan tail update: the `0x8C14D158-0x8C14D15C` empty callback scan now fast-forwards from the post-increment and post-compare tails, preserving the pending-mask and final callback-scan state. Focused callback-scan tests and the full 1233-test suite pass. The 300M SA2 probe `sa2-callback-scan-increment-tail-300m-20260612-215745` still lands at `PC=0x8C15BF8E`, but CPU fast-forward rises to `267,605,291`; the callback compare edge `0x8C14D15A` drops from `78,269` hits to `52,180`, matching the remaining once-per-cycle callback scan pressure.
- AICA name-loop from-entry exit coverage: post-setup returns to `0x8C15B92E` can now bridge directly through the name-loop exit and next-group setup path when the restored name-loop index is already at the group boundary. Focused name-loop/post-setup tests and the full 1235-test suite pass. The 300M SA2 probe `sa2-name-loop-entry-exit-300m-20260612-223957` is unchanged from the prior callback-scan run (`PC=0x8C15BF8E`, `cpuFastForward=267,605,291`), so this closes a parity gap but is not the current hot `0x8C15B92E` source.
- SR callback-list prologue update: the `0x8C10FE72-0x8C10FE84` dispatcher prologue now fast-forwards the SR mask, saved `PR/r14` frame, and callback-list head load before landing at the existing `0x8C10FE8E` tail helpers. Focused SR callback-list tests and the full 1237-test suite pass. The 300M SA2 probe `sa2-sr-callback-prologue-300m-20260612-225209` still lands at `PC=0x8C15BF8E`, but CPU fast-forward rises to `267,840,101`; the remaining top profile pressure is now mostly trigger PCs around the same service-loop cadence rather than this dispatcher prologue body.
- AICA active post-setup name-loop continuation update: active-channel post-setup returns can now continue through the next name-loop setup from the restored `0x8C15B92E` state instead of stopping after the flag-return epilogue. Focused active post-setup tests and the full 1238-test suite pass. The 300M SA2 probe `sa2-active-postsetup-name-loop-300m-20260612-230152` is unchanged from the SR callback prologue run (`PC=0x8C15BF8E`, `cpuFastForward=267,840,101`), so this is another parity/coverage closure rather than the live service-loop source.
- AICA mode-1 descriptor wrapper update: the `0x8C15CA78-0x8C15CA8E` mode-1 wrapper now fast-forwards its saved-`PR` frame, zero arguments, descriptor-update call setup, and return epilogue when the nested descriptor path can complete, while reporting partial descriptor paths with correct scheduler accounting. Focused descriptor-wrapper tests and the full 1239-test suite pass. The 300M SA2 probe `sa2-mode1-descriptor-wrapper-300m-rebuilt-20260612-232351` still lands at `PC=0x8C15BF8E`, but CPU fast-forward rises to `268,257,537`; the wrapper body `0x8C15CA7A-0x8C15CA8C` drops out of the top profile, leaving `0x8C15CA78` as a trigger PC plus the existing descriptor/setup edges.
- AICA mode-9 descriptor wrapper update: the `0x8C15CA16-0x8C15CA32` pending-mask wrapper now fast-forwards its stack mask argument setup, descriptor-update call entry, and any reachable nested descriptor partial path with correct scheduler accounting. Focused descriptor-wrapper tests and the full 1241-test suite pass. The 300M SA2 probe `sa2-mode9-descriptor-wrapper-300m-20260612-233330` still lands at `PC=0x8C15BF8E`, but CPU fast-forward rises to `268,544,516`; this removes another wrapper setup layer from the mode-9 slot path while leaving the deeper descriptor extra-word path as the next exposed edge.
- AICA DMA descriptor completion-marker update: the exact `0x8C15CE5C-0x8C15CE6E` helper now fast-forwards the Maple/AICA descriptor completion mark by OR-ing `0x80000000` into the descriptor word reached through `[*0x8C2A34F8 + 0x0FCC]`, preserving the cached system-RAM address and return-zero state. Focused parity/guard tests and the full 1272-test suite pass. The 50M SA2 probe `sa2-descriptor-complete-marker-50m-20260613-112646` still lands at `PC=0x8C15C7F4` with no GD-ROM reads or TA writes, CPU fast-forward rises to `49,313,987`, and `0x8C15CE5E-0x8C15CE6E` drop out of the hot profile, leaving `0x8C15CE5C` as the single trigger PC.
- Interrupt callback-list prologue update: the exact `0x8C10FE4E-0x8C10FE5E` source-list dispatcher prologue now fast-forwards the saved `r14/PR` frame and table-head lookup for source `0xA00`, then lands at `0x8C10FE68` so the real callback bodies still execute normally. Focused parity/guard tests and the full 1275-test suite pass. The 50M SA2 probe `sa2-callback-list-prologue-50m-20260613-113345` still lands at `PC=0x8C15C7F4`, CPU fast-forward rises to `49,318,891`, and `0x8C10FE50-0x8C10FE5E` drop out of the hot profile, leaving `0x8C10FE4E` as the trigger.
- IRQB source-decode prologue update: the exact `0x8C10FEE2-0x8C10FF1E` interrupt-source decoder now fast-forwards the saved-register frame, SR mask, and ASIC pending/mask reads for event `0x360`, then lands at `0x8C10FF20` so the real source bit-scan still runs. Focused parity/guard tests and the full 1278-test suite pass. The 50M SA2 probe `sa2-irqb-source-decode-prologue-50m-20260613-114101` still lands at `PC=0x8C15C7F4` with no GD-ROM reads or TA writes, CPU fast-forward rises to `49,334,829`, and `0x8C10FEE4-0x8C10FF1E` drop out of the hot profile, leaving `0x8C10FEE2` plus the real `0x8C10FF20` bit-scan cluster.
- IRQB first-dispatch prefix update: the exact `0x8C10FF20-0x8C10FF40` first-source path now fast-forwards only when the compressed IRQB mask is exactly bit 0, setting up source `0xA00`, `PR=0x8C10FF42`, and landing at the real `0x8C10FE4E` callback-list dispatcher. Focused parity/guard tests and the full 1281-test suite pass. The 50M SA2 probe `sa2-irqb-first-dispatch-prefix-50m-20260613-114545` still lands at `PC=0x8C15C7F4`, CPU fast-forward rises to `49,347,089`, `0x8C10FF22-0x8C10FF40` drop out of the profile, and the newly exposed once-per-cycle callback body is `0x8C135C60...0x8C135CA6`.
- AICA counter-callback return-tail update: the exact `0x8C135C70-0x8C135C9E` post-helper tail now fast-forwards only while the normal counter at `0x8C29BED4` is `<= 60`, restoring saved `PR/r14` and returning to the callback dispatcher. Focused parity/guard tests and the full 1284-test suite pass. The 50M SA2 probe `sa2-aica-counter-callback-return-tail-50m-20260613-115043` still lands at `PC=0x8C15C7F4`, CPU fast-forward rises to `49,351,380`, and the body after trigger `0x8C135C70` is retired; the next exposed callback pressure is now around `0x8C135CA0...0x8C135CB4`.
- AICA masked-callback prologue update: the exact `0x8C135CA0-0x8C135CBA` second callback prologue now fast-forwards the saved `r14/r13/r12/r11/PR` frame, SR mask, and zero flag check at `0x8C29BECC`, then lands at `0x8C135CBC` so the real counter increment and helper calls still run. Focused parity/guard tests and the full 1287-test suite pass. The 50M SA2 probe `sa2-aica-callback-masked-prologue-50m-20260613-120152` still lands at `PC=0x8C15C7F4`, CPU fast-forward rises to `49,359,349`, and `0x8C135CA2-0x8C135CBA` drop out of the profile, exposing `0x8C135CBC...0x8C135CC6` and the deeper `0x8C136042` helper.
- AICA masked-callback counter-call update: the exact `0x8C135CBC-0x8C135CC6` bridge now fast-forwards the `0x8C29BC28` counter increment and `bsr` setup, landing at the real `0x8C1362FC` helper with `PR=0x8C135CC8`. Focused parity/guard tests and the full 1290-test suite pass. The 50M SA2 probe `sa2-aica-callback-counter-call-50m-20260613-120719` still lands at `PC=0x8C15C7F4`, CPU fast-forward rises to `49,362,414`, and the bridge body drops out, leaving the deeper `0x8C136042`/`0x8C1362FC` helpers as the next callback-side targets.
- AICA service-helper final counter-tail update: the exact `0x8C1361F0-0x8C13622C` final service-helper epilogue now fast-forwards the `0x8C29BC1C` counter increment, clears the normal-counter scratch at `0x8C29BED4`, restores `PR/r9-r14`, and returns to the outer callback tail. Focused parity/guard tests and the full 1293-test suite pass. The 50M SA2 probe `sa2-aica-service-final-counter-tail-50m-20260613-121848` still lands at `PC=0x8C15C7F4`, CPU fast-forward rises to `49,370,383`, and the final tail drops out of the hot profile; the next callback-side targets are the larger `0x8C136042` helper body and the `0x8C1362FC` helper.
- AICA callback-service prologue update: the exact `0x8C1362FC-0x8C13632C` helper prefix now fast-forwards the saved `r14-r8/PR` frame, local interrupt-priority save, SR mask, and call setup into the real `0x8C15B87A` helper. Focused parity/guard tests and the full 1296-test suite pass. The 50M SA2 probe `sa2-aica-callback-service-prologue-50m-20260613-122427` still lands at `PC=0x8C15C7F4`, CPU fast-forward rises to `49,385,095`, and `0x8C1362FE-0x8C13632C` leaves the hot profile; the next exposed callback body remains the larger `0x8C136042` service helper plus nested AICA/name helper triggers.
- AICA service-helper flagged-prologue update: the exact `0x8C136042-0x8C136056` helper prefix now fast-forwards the saved `r14-r9/PR` frame and branch to `0x8C136070` only when `0x8C29BE9C` is nonzero, leaving the zero-flag fallthrough path real. Focused parity/guard tests and the full 1299-test suite pass. The 50M SA2 probe `sa2-aica-service-flagged-prologue-50m-20260613-123410` still lands at `PC=0x8C15C7F4`, CPU fast-forward rises to `49,391,225`, and `0x8C136044-0x8C136056` leaves the hot profile; the next exposed part of this helper is the state-reset block at `0x8C136070...0x8C13607C`.
- AICA service-helper first-reset update: the exact `0x8C136070-0x8C136086` reset block now fast-forwards the `0x8C29BEB4 -> 0` and `0x8C29BEB0 = old - 1` updates, mask load, and branch to `0x8C13609C` only while `0x8C29BEAC` is zero. Focused parity/guard tests and the full 1302-test suite pass. The 50M SA2 probe `sa2-aica-service-first-reset-block-50m-20260613-123832` still lands at `PC=0x8C15C7F4`, CPU fast-forward rises to `49,397,968`, and `0x8C136072-0x8C136086` leaves the hot profile; the next exposed helper chunk is `0x8C13609C...0x8C1360A4`, followed by the AICA register-read setup at `0x8C1360BA`.
- AICA service-helper second-reset update: the exact `0x8C13609C-0x8C1360A4` reset/test block now fast-forwards the `0x8C29BEAC -> 0` store and branch to `0x8C1360BA` only while `0x8C29BEA8` is zero. Focused parity/guard tests and the full 1305-test suite pass. The 50M SA2 probe `sa2-aica-service-second-reset-block-50m-20260613-161111` still lands at `PC=0x8C15C7F4`, CPU fast-forward rises to `49,400,420`, and `0x8C13609E-0x8C1360A4` leaves the hot profile; the next exposed helper chunk is `0x8C1360BA...0x8C1360C2`, where the helper reads `AICA 0xA05F6908`.
- AICA service-helper status-branch update: the exact `0x8C1360BA-0x8C1360C6` ASIC event-bank-C status test now fast-forwards the observed branch-taken path when `0xA05F6908 & 0x0F00 == 0`, preserving the loaded status/register literals and landing at `0x8C1361BA`. Focused parity/guard tests and the full 1308-test suite pass. The 50M SA2 probe `sa2-aica-service-status-branch-50m-20260613-161811` still lands at `PC=0x8C15C7F4` with no GD-ROM reads or TA writes, CPU fast-forward rises to `49,404,098`, and `0x8C1360BC-0x8C1360C6` leaves the hot profile; the next exposed helper chunk is `0x8C1361BA...`.
- AICA service-helper post-status mirror update: the exact `0x8C1361C0-0x8C1361EA` post-call update now fast-forwards the nonnegative service result path, replaying the `0x8C29BECC` scratch store, the two `0x8C29BC48/50 + index*2` mirror writes, and the `0xA05F810C` PVR sync-status read before landing at the existing mirror-copy call fast-forward. Focused parity/guard tests and the full 1312-test suite pass. The 50M SA2 probe `sa2-aica-service-post-status-mirror-50m-20260613-162655` still lands at `PC=0x8C15C7F4` with no GD-ROM reads or TA writes, CPU fast-forward rises to `49,416,971`, and `0x8C1361C2-0x8C1361EA` leaves the hot profile; the remaining service-helper pressure is now trigger/handoff PCs `0x8C1361BA`, `0x8C1361C0`, `0x8C1361EC`, and `0x8C1361F0`.
- Interrupt callback status quiet-path update: the exact `0x8C14C860-0x8C14CB7A` quiet callback path now fast-forwards the status-callback prologue, ASIC event-ack write, callback/toggle counters, entry-zero status mark, and inactive entries one through three before landing at `0x8C14CB80`. Focused parity/guard tests and the full 1315-test suite pass. The 50M SA2 probe `sa2-interrupt-status-quiet-path-50m-20260613-164630` still lands at `PC=0x8C15C7F4` with no GD-ROM reads or TA writes, CPU fast-forward rises to `49,489,918`, and `0x8C14C862-0x8C14CB7A` leaves the hot profile; the next exposed callback-status body is `0x8C14CB80...`.
- Interrupt callback status return-tail update: the exact `0x8C14CB80-0x8C14CBBC` status callback epilogue now fast-forwards the merged status local, saved-register restore, and return through the saved `PR`, handing control directly back to the outer IRQ status decoder. Focused parity/guard tests and the full 1318-test suite pass. The 50M SA2 probe `sa2-interrupt-status-return-tail-50m-20260613-165100` still lands at `PC=0x8C15C7F4` with no GD-ROM reads or TA writes, CPU fast-forward rises to `49,500,952`, and `0x8C14CB82-0x8C14CBBC` leaves the hot profile; the next exposed callback-status body is the outer decoder at `0x8C14CEB0...`.
- Interrupt callback status decoder-prefix update: the exact `0x8C14CED2-0x8C14D152` post-status decoder path now fast-forwards the deterministic event-bit checks, bank-C read, pending-bit-1 clear, and callback argument setup, landing at the real `0x8C14D154` dispatch call so the callback-list body still executes normally. Focused parity/guard tests and the full 1321-test suite pass. The 50M SA2 probe `sa2-interrupt-status-decoder-prefix-50m-20260613-171327` still lands at `PC=0x8C15C7F4` with no GD-ROM reads or TA writes, CPU fast-forward rises to `49,549,379`, and `0x8C14CED4-0x8C14D152` leaves the hot profile; the next exposed body is the callback-list dispatch at `0x8C14D154/0x8C14D1F4...`.
- Interrupt callback FPU-wrapper prologue update: the exact `0x8C14D1F4-0x8C14D22A` wrapper now fast-forwards the FPSCR/FR/PR save frame and delay-slot callback argument move, then lands at the real `0x8C10FE72` SR callback-list dispatcher. Focused parity/guard tests and the full 1325-test suite pass. The 50M SA2 probe `sa2-interrupt-callback-fpu-wrapper-50m-20260613-173025` still lands at `PC=0x8C15C7F4` with no GD-ROM reads or TA writes, CPU fast-forward rises to `49,565,930`, and the wrapper body leaves the hot profile; the next high-value targets are again the SA2 AICA name/slot clusters around `0x8C15B92E`, `0x8C15B918`, `0x8C15C57C`, plus the remaining IP.BIN trigger `0x8C0089F6`.
- AICA descriptor source-guard update: the active-channel descriptor, zero-mask descriptor-copy, and descriptor-copy helper fast-forwards now allow readable descriptor sources from system-RAM aliases and AICA RAM under the same opcode/state guards. Focused P2/AICA descriptor-source parity tests and the full 1329-test suite pass. The 50M SA2 probe `sa2-descriptor-source-guard-50m-20260613-174420` is unchanged at `PC=0x8C15C7F4`, with no GD-ROM reads or TA writes and CPU fast-forward still `49,565,930`; this closes source-address coverage rather than moving the active budget frontier.
- AICA zero-mask post-setup chain update: the name-loop zero-mask aggregate now consumes the immediate `0x8C15C57C` post-setup entry when budget allows, using the existing post-setup return helper before handing back to the next name-loop trigger. Focused parity tests pass. The 50M SA2 probe `sa2-zero-mask-postsetup-chain-50m-20260613-175603` still lands at `PC=0x8C15C7F4` with no GD-ROM reads or TA writes, CPU fast-forward rises to `49,566,542`, and `0x8C15C57C` drops from `1838` to `1226` samples; `0x8C15B92E` remains the top AICA name-loop trigger.
- AICA active descriptor continuation update: post-setup returns that re-enter the name loop now continue through the next active channel descriptor-return path instead of stopping at the active setup boundary. Focused parity tests and the full 1335-test suite pass. The 50M SA2 probe `sa2-name-loop-active-descriptor-chain-50m-20260613-181729` still lands at `PC=0x8C15C7F4` with no GD-ROM reads or TA writes, but CPU fast-forward rises to `49,571,442`; the former `0x8C15B92E` top trigger drops out of the top profile slice, leaving Maple/device polling, IP.BIN glyph trigger `0x8C0089F6`, and the next AICA tail/descriptor trigger PCs as the visible pressure.
- IP.BIN zero-to-set glyph dispatch update: the zero-bit glyph aggregate now consumes the next set-bit dispatch/draw path when a collapsed zero run lands back at `0x8C0089F6` with budget remaining. Focused glyph parity tests and the full 1336-test suite pass. The 50M SA2 probe `sa2-ipbin-zero-set-dispatch-chain-50m-20260613-182245` still lands at `PC=0x8C15C7F4` with no GD-ROM reads or TA writes, but CPU fast-forward rises to `49,572,110`; the `0x8C0089F6` trigger drops from `4249` to `3581` samples.
- IP.BIN byte-exit chain update: zero-bit glyph runs that reach the byte-exit trigger now continue through the byte-exit and next byte-fetch path from entry when budget allows. Focused glyph parity tests and the full 1337-test suite pass. The 50M SA2 probe `sa2-ipbin-byte-exit-chain-50m-20260613-182807` still lands at `PC=0x8C15C7F4` with no GD-ROM reads or TA writes, but CPU fast-forward rises to `49,573,070` and `0x8C008A76` drops out of the top profile slice.
- IP.BIN loop-tail dispatch coverage: the public glyph loop-tail helper now consumes an immediate next bit-dispatch when it lands at `0x8C0089F6` with budget remaining. Focused glyph parity tests and the full 1338-test suite pass. The 50M SA2 probe `sa2-ipbin-loop-tail-dispatch-chain-50m-20260613-202249` is unchanged from the byte-exit run (`PC=0x8C15C7F4`, CPU fast-forward `49,573,070`), so the remaining live `0x8C0089F6` pressure comes from other entry paths.
- AICA name-group descriptor-head setup chain update: direct entries at `0x8C15B8EC` now chain through the immediate active-mask or zero-mask name-call setup paths instead of stopping at `0x8C15B918`. Focused parity tests and the full 1341-test suite pass. The 50M SA2 probe `sa2-name-head-setup-chain-50m-20260613-204515` still lands at `PC=0x8C15C7F4` with no GD-ROM reads or TA writes, but CPU fast-forward rises to `49,573,683`, batches drop to `40,618`, and `0x8C15B918` drops from `1838` to `1225` samples.
- AICA mode-1 descriptor return-tail chain update: active mode-1 slot descriptor updates now continue through the `0x8C15B652` return bridge, execute the `0x8C15B686` slot-stride increment, and hand into the existing no-work scan tail when the following slots are inactive. Focused parity tests and the full 1342-test suite pass. The 50M no-trace SA2 probe `sa2-mode1-return-tail-chain-notrace-50m-20260613-205711` advances past the former `PC=0x8C15C7F4` wall to `PC=0x8C135BFC`, reaches 45 successful GD-ROM reads plus 2 TOC requests, and raises CPU fast-forward to `49,804,111` over only `6,524` batches. The next visible pressure is no longer the AICA name/slot loop; it is the IP.BIN glyph trigger `0x8C0089F6`, record parser spans around `0x8C134DB4-0x8C134ED0`, small copy loops around `0x8C11055E`/`0x8C0C10EE`, and an AICA text/helper cluster around `0x8C15C9D0`.
- Record-name parser entry-tail update: direct entries to the `0x8C134DB4` record-name parser now fast-forward after the first saved-register push, synthesizing the remaining prologue and reusing the proven parser body shortcut. Focused parser parity tests and the full 1343-test suite pass. The 50M SA2 probe `sa2-record-name-entry-tail-50m-20260613-214810` remains at `PC=0x8C135BFC` with 45 successful GD-ROM reads and 2 TOC requests, but CPU fast-forward rises to `49,812,806`; `0x8C134DB6-0x8C134DBE` drop out of the top profile, leaving the first real trigger `0x8C134DB4` and the separate nearby `0x8C134EAA...0x8C134ED0` span as the next record-helper target.
- Record-name walk-loop update: the `0x8C134EAA-0x8C134ED0` record walk now aggregates repeated valid record parses, advances source/destination/index state, and stops cleanly at `0x8C134EAA` when the next short record needs the real parser path. Focused record-name tests and the full 1344-test suite pass. The 50M SA2 probe `sa2-record-name-walk-prefix-50m-20260613-230305` remains at `PC=0x8C135BFC` with 45 successful GD-ROM reads and 2 TOC requests, but CPU fast-forward rises to `49,842,312`, fast-forward batches drop to `4,829`, and the whole `0x8C134DB4/0x8C134EAA...0x8C134ED0` record-walk band leaves the top profile. The next visible clusters are the IP.BIN glyph paths around `0x8C0089F6`/`0x8C0086A8`, small copy/fill spans around `0x8C11055E` and `0x8C0C10D6-0x8C0C10F6`, plus AICA helper spans around `0x8C15C9D0` and `0x8C16B478`.
- Aligned word zero-fill update: the SA2 helper loop at `0x8C11055E-0x8C110568` now fast-forwards aligned zero-fill spans, including the pending delayed-slot store and the aligned tail bookkeeping through `0x8C110582`. Focused parity/guard tests and the full 1347-test suite pass; the desktop app also builds in Release. The 50M SA2 probe `sa2-aligned-word-zero-fill-50m-20260614-005324` remains at `PC=0x8C135BFC` with 45 GD-ROM reads and 2 TOC requests, but CPU fast-forward rises to `49,846,250`, and the `0x8C11055E-0x8C110568` band leaves the top profile. The next visible clusters are IP.BIN glyph paths around `0x8C0089F6`/`0x8C0086A8`, the `0x8C0C10D6-0x8C0C10F6` copy/fill spans, and AICA helper spans around `0x8C15C9D0` and `0x8C16B478`.
- Small word copy-table update: the `0x8C0C1098` jump-table helper now fast-forwards the observed 8/16/24/32-byte word-copy paths, preserving the helper's final `r0`, restored `r3`, stack, and return state. Focused parity/guard tests and the full 1351-test suite pass; the desktop app also builds in Release. The 50M SA2 probe `sa2-small-word-copy-table-50m-20260614-013944` remains at `PC=0x8C135BFC` with 45 GD-ROM reads and 2 TOC requests, CPU fast-forward rises to `49,858,154`, and the `0x8C0C10D6-0x8C0C10F6` body band leaves the top profile. The one-instruction `0x8C0C1098` trigger remains visible because the shortcut starts after its saved-register push. The next visible clusters are IP.BIN glyph paths around `0x8C0089F6`/`0x8C0086A8`, the AICA helper spans around `0x8C15C9D0` and `0x8C16B478`, and possible chaining from this copy helper's callers.
- AICA work-queue copy-table caller update: the `0x8C16B478-0x8C16B48E` caller loop now aggregates repeated 32-byte rows through the `0x8C0C1098` copy-table helper after the first pointer load, preserving the helper residual stack word and final loop registers. Focused parity/guard tests and the full 1354-test suite pass; the desktop app also builds in Release. The 50M SA2 probe `sa2-aica-workqueue-copy-table-loop-50m-20260614-020021` remains at `PC=0x8C135BFC` with 45 GD-ROM reads and 2 TOC requests, CPU fast-forward rises to `49,864,297`, and the `0x8C16B478` caller band leaves the top profile. The next visible clusters are IP.BIN glyph paths around `0x8C0089F6`/`0x8C0086A8`, the AICA helper span around `0x8C15C9D0`, and the long PVR/register copy loop currently ending at `0x8C135BFC`.
- G2 PIO write entry-tail update: the `0x8C135BC0-0x8C135C16` AICA RAM upload entry now fast-forwards the first validated word write and chains into the existing cheap write-loop shortcut for remaining contiguous words. Focused entry/body tests and the full 1357-test suite pass; the desktop app also builds in Release. The 50M SA2 probe `sa2-g2-pio-write-entry-tail-50m-20260614-021145` remains at `PC=0x8C135BFC` with 45 GD-ROM reads and 2 TOC requests, CPU fast-forward rises to `49,864,403`, and the real fixed-budget pressure is now the remaining AICA RAM upload plus the IP.BIN glyph and `0x8C15C9D0` helper clusters.
- AICA descriptor-name space-fill update: the `0x8C15C9D0-0x8C15C9DA` descriptor-name padding loop now fast-forwards the remaining system-RAM space fill after the first register move, preserving final `r3/r4/T` state. Focused parity/guard tests and the full 1359-test suite pass; the desktop app also builds in Release. The 50M SA2 probe `sa2-aica-name-space-fill-50m-20260614-023127` remains at `PC=0x8C135BFC` with 45 GD-ROM reads and 2 TOC requests, CPU fast-forward rises to `49,867,979`, and the `0x8C15C9D0` cluster leaves the top profile. The remaining top pressure is now almost entirely IP.BIN glyph work plus smaller helper clusters around `0x8C10FC70`, `0x8C14DAA4`, and `0x8C10FC80`.
- IP.BIN glyph table-scan update: the `0x8C0086A8-0x8C0086DE` three-byte glyph descriptor scan now fast-forwards both match and scan-to-limit exits after the loop branch proves `index < limit`, preserving the signed target-byte compare, stack pointer/accumulator/index writes, and final branch target state. Focused parity/guard tests and the full 1362-test suite pass; the desktop app also builds in Release. The 50M SA2 probe `sa2-ipbin-glyph-table-scan-50m-20260614-193412` remains at `PC=0x8C135BFC` with 45 GD-ROM reads and 2 TOC requests, CPU fast-forward rises to `49,879,858`, and the `0x8C0086A8` table-scan body leaves the top profile. The remaining fixed-budget pressure is led by the `0x8C0089F6` glyph bit-dispatch trigger, `0x8C008A76` byte-exit trigger, IP.BIN pattern-fill tail at `0x8C008F48-0x8C008F4E`, and smaller SA2 helper clusters around `0x8C10FC70`.
- Interrupt mask-table clear update: the `0x8C10FC70-0x8C10FC76` stride-16 zero-store loop now fast-forwards the remaining system-RAM entries after the first taken branch, preserving final `r4 == r6`, `T=1`, and the fallthrough at `0x8C10FC78`. Focused parity/guard tests and the full 1365-test suite pass; the desktop app also builds in Release. The 50M SA2 probe `sa2-interrupt-mask-table-clear-50m-20260614-194901` remains at `PC=0x8C135BFC` with 45 GD-ROM reads and 2 TOC requests, CPU fast-forward rises to `49,880,878`, and the `0x8C10FC70-0x8C10FC76` band leaves the top profile. The remaining fixed-budget pressure is now mostly IP.BIN glyph dispatch/byte-exit and the pattern-fill body.
- Interrupt pending-table clear update: the `0x8C10FC80-0x8C10FC86` contiguous zero-store loop now fast-forwards the remaining system-RAM words after the first taken branch, preserving final `r4 == r6`, `T=1`, and the fallthrough at `0x8C10FC88`. Focused parity/guard tests and the full 1368-test suite pass; the desktop app also builds in Release. The 50M SA2 probe `sa2-interrupt-pending-table-clear-50m-20260614-195754` remains at `PC=0x8C135BFC` with 45 GD-ROM reads and 2 TOC requests, CPU fast-forward rises to `49,881,642`, and the `0x8C10FC80-0x8C10FC86` band leaves the top profile. The remaining fixed-budget pressure is now IP.BIN glyph dispatch/byte-exit plus the IP.BIN pattern-fill body.

- AICA no-work setup-tail update: the `0x8C15B5B6` setup tail now completes the post-literal save path and chains into the existing no-work slot scan when the work counter is nonpositive. Focused AICA no-work tests pass; a 500002000-instruction SA2 sanity probe advances from the former `0x8C15B5B6` frontier to `PC=0x8C15C094`, with 51 GD-ROM commands observed, no TA writes yet, and `497,550,588` CPU-fast-forwarded instructions.
- AICA channel-setup pre-callback update: the `0x8C15C512` bridge tail now covers the short setup span before the existing callback-tail helper. Focused channel-setup tests pass; a 500010080-instruction SA2 sanity probe advances from the former `0x8C15C512` frontier to `PC=0x8C15C666`, with `497,557,963` CPU-fast-forwarded instructions.
- AICA descriptor-copy inactive branch update: the `0x8C15C666` inactive descriptor branch tail now writes the slot status word and returns through the descriptor helper frame after the `0xFF` descriptor path is proven. Focused descriptor-copy tests pass; a 500020000-instruction SA2 sanity probe advances to the G2/AICA caller region at `PC=0x8C15B24E`, with `497,567,558` CPU-fast-forwarded instructions.
- G2 DMA inactive-status wrapper update: the `0x8C15B24E` wrapper tail now folds the tiny `0x8C170BBC` status probe, increments the observed `tableBase+0x10C` counter, and restores the wrapper frame when `A05F688C & 0x10 == 0`. Focused wrapper tests pass; a 500040000-instruction SA2 sanity probe advances to the generic G2 PIO read prologue at `PC=0x8C1356DE`, with `497,590,003` CPU-fast-forwarded instructions.
- G2 PIO read mid-prologue update: the `0x8C1356DE` partial prologue now synthesizes the remaining saved-register frame and chains into the existing G2 PIO read helper shortcut. Focused G2 PIO read tests pass; a 500060000-instruction SA2 sanity probe advances to `PC=0x8C16B598`, with `497,609,898` CPU-fast-forwarded instructions and no TA writes yet.
- G2 DMA status-set read-tail update: the `0x8C170AAE` helper tail now covers the post-read budget edge after the channel index has been loaded from the caller's table entry. Focused G2 status helper tests pass; a 500061020-instruction SA2 sanity probe clears the former `PC=0x8C170AB0` boundary and returns to the caller loop, and a 500063000-instruction probe reaches `PC=0x8C15B270` with no TA writes yet.

## Work Plan

1. Keep `tools\probe-sonic.ps1 -AssertKnownFrontiers` as the quick regression loop while this milestone is active.
2. For Sonic Adventure 2, continue the AICA startup path: identify which AICA RAM status words are guest/ARM mailboxes, model the minimal command-completion semantics, then re-run higher budgets until either PVR TA traffic appears or a new renderer gate is exposed.
3. For Sonic Shuffle, trace why the boot work-area mode bytes at `0x8C0080FC/FE` remain `1` before the branch to `0x8C008300`; compare the path against BIOS handoff and soft-reset behavior before forcing any global boot byte.
4. For Sonic Adventure, trace the repeated sector `45166` reads and the state that leads to BIOS system function `1`; inspect the loaded read target, status buffers, and callback/table state around `0x8C60434C-0x8C604350`.
5. After each title passes its current frontier, use GD-ROM command logs, PVR TA logs, and framebuffer snapshots to chase the next intro-sequence checkpoint.

## Success Criteria

The milestone is not complete when the games merely reach a higher instruction count. It is complete when the Sonic probes show visible intro/title progress with at least one of:

- sustained successful GD-ROM reads after the current frontiers,
- nonzero PVR TA command traffic,
- a nonblank framebuffer or intro/title visual output.
