# dcSharp Roadmap

## Milestone 0: Tools And Legal Fixtures

- KallistiOS installed and verified in WSL.
- Repo can build a tiny KOS ELF fixture.
- CLI can inspect ELF identity and Dreamcast plausibility.
- Docs define legal asset policy and reference sources.

## Milestone 1: Loader And Memory

- Parse ELF32 SH executable headers and program headers.
- Load segments into Dreamcast system RAM through P1/P2 address mirrors.
- Provide deterministic memory reads/writes with trace hooks.
- Add open-bus/unmapped-access diagnostics without crashing the host.
- CLI can produce a loader summary for a KOS ELF.

## Milestone 2: SH-4 Interpreter Slice

- Implement core integer/register model. Done for the current KOS fixtures.
- Decode 16-bit opcodes with trace formatting. Done for the current KOS fixtures.
- Support branches, delay slots, loads/stores, arithmetic, comparisons, basic system registers, `trapa` entry, modeled `0xFFFD` illegal-instruction exceptions, slot-illegal PC/SR-changing delay-slot exceptions, and `rte` return. Done for the current KOS fixtures, with bare-metal trap and illegal-instruction fixtures pinning exception diagnostics; SH-4 `FPSCR.FR` changes now swap the active FR/XF floating-point register banks.
- Stop cleanly on unsupported opcode or exception. Done.
- Keep adding opcodes only when a fixture or focused unit test exposes the missing behavior.

## Milestone 3: KOS Homebrew Reaches Main

- Model enough boot/runtime expectations for `samples/kos/hello`. Done.
- Firmware HLE includes sysinfo/flash return-zero stubs, GD-ROM request handling, retail boot-mode work-area seeds, and named terminal system BIOS traps for reset/menu requests.
- Capture `printf` or serial-like output through an explicit diagnostic channel. Done: SCIF transmit writes are captured as serial bytes.
- Emit a run summary with final PC, executed instructions, memory faults, serial output, trace tail, and device accesses. Done for text and JSON CLI output.
- Current KOS probe frontier: default KOS init reaches `main()`, prints both probe messages, runs shutdown, and stops as `ProgramExit`.
- Symbolize stop PCs and trace-tail entries from ELF symbol tables when available. Done.
- Add bounded trace and device-access log filters for focused debugging. Done.

## Milestone 4: Timers, Interrupts, And Maple Probe

- Add scheduler clock and interrupt controller basics. Started with periodic VBlank, ASIC event masks, SH-4 external interrupt entry with `SR.BL`/`SR.IMASK`/delay-slot coverage, TMU countdown/reload, IPRA priority lookup, TMU/ASIC pending-source arbitration, and focused full/partial fast-forward paths for known long idle/fill/copy loops.
- Build KOS fixtures for timer callbacks and controller polling. Started: `samples/kos/timer` uses `timer_ms_gettime64()` and `thd_sleep()`; `samples/kos/timer_callback` chains the TMU0 primary timer callback; `samples/kos/timer_vblank` observes TMU callback delivery while VBlank IRQ9 is enabled; `samples/kos/asic_irqb` leaves a Maple DMA IRQB source pending; `samples/kos/maple_irqb_accept` accepts and clears Maple DMA IRQB through the SH-4 external interrupt vector; `samples/kos/asic_events` observes and clears an ASIC VBlank event latch; `samples/kos/asic_irq9_masked` leaves VBlank IRQ9 pending while SH-4 interrupts are masked; `samples/kos/interrupt_nesting` confirms `SR.BL` blocks nested IRQ9 delivery while a VBlank source remains pending in the handler; `samples/kos/timer_asic_arbitration` records TMU0 before IRQ9 when both sources are pending and TMU0 has higher priority; `samples/kos/timer_irq_masked` leaves TMU0 pending while SH-4 interrupts are masked; `samples/kos/timer_irq_accept` accepts and clears a pending TMU0 interrupt through the external interrupt vector; `samples/kos/vblank_idle` observes an idle wake on synthetic VBlank; `samples/kos/maple_controller` detects a virtual controller; `samples/kos/maple_controller_script` observes an instruction-indexed input transition; `samples/kos/input_idle` observes an idle wake on an input-script change; `samples/kos/maple_controller_b` covers absent/configured B0 behavior.
- Keep KOS fixture expectations in a manifest and run them through a shared CLI/test validator. Done, including fixture filtering for targeted CLI and `tools/check.ps1` runs.
- Centralize TMU advancement and the synthetic VBlank pulse in `DreamcastEventScheduler`. Done.
- Expose scheduler event diagnostics in run summaries. Done.
- Expose ASIC event register and pending interrupt diagnostics in run summaries. Done, including decoded pending source register/bit and A/B/C event-bank ACK coverage.
- Add fixture expectations for ASIC event state. Started: manifests can assert no pending ASIC interrupt plus ACK/mask/pending event registers.
- Expose Maple DMA command/response diagnostics in run summaries. Done, including per-DMA descriptor traversal and descriptor-limit diagnostics.
- Expose aggregate device-access domain and access-kind diagnostics in run summaries. Done.
- Add fixture expectations for aggregate device-domain access counts. Done.
- Improve the scheduler to coalesce timer advancement. Done for skipped instruction-count calls, SH-4 `sleep` waits, side-effect-free self-branch waits, narrow read-only interruptible `bt`/`bf` polling waits, controller-script wake boundaries, and masked `dt`/`bf/s` counted-delay loops. Live fixtures now pin timer, VBlank, and input idle-wake reasons.
- Add frame/input scripts that vary Maple controller state over instruction time. Started with generic `--controller address:state` and `--controller-script address:script` mapping, compatibility A0/B0 shorthands, optional B0 controller configuration, raw Maple condition transition fixtures, and an input-idle wake fixture.

## Milestone 5: Video And Audio Bring-Up

- Start with framebuffer/register visibility. Started: PVR VRAM apertures are backed; `samples/kos/framebuffer` writes a RGB565 pattern and `samples/kos/video_mode` sets 640x480 RGB565 with sentinel pixels.
- Add the first tiny software preview path. Started: assembled opaque, punch-through, and translucent TA strips can draw small RGB565 triangle previews into VRAM for flat-color, Gouraud-color, and multi-triangle strip fixtures, with selected depth, blend, alpha discard, UV, filter, and texture format behavior.
- Dump the current RGB565 framebuffer snapshot to PNG for visual fixture checks. Done.
- Add PVR command logging before a full renderer. Started: current named PVR register values, named register accesses, TA command writes, grouped TA lists, and assembled opaque TA strips are captured in video summaries.
- Classify TA command writes before a full renderer. Started with first-word command kind, list type decoding, a real TA parameter decoder skeleton, a TA stream control/payload diagnostic view with named polygon/sprite/modifier/user-clip payload slots, real-shaped KOS polygon header/vertex and sprite-header/sprite-geometry fixtures with zero and nonzero mode payloads, a tiny diagnostic strip assembler that consumes fixture-only control/X/Y/color vertex packets across renderable lists, mixed-color Gouraud strip assembly for matching headers, immediate write-order strip/sprite preview composition, and tiny rectangular/skewed sprite quad previews with face-color plus non-twiddled/twiddled RGB565, ARGB4444-alpha, shading, and UV-mode texture paths.
- Add fixture expectations for PVR state. Started: manifests can assert current named PVR register values, RGB565 sentinel samples, TA command/list counts, and assembled TA strip matches.
- Add fixture expectations for AICA state. Started: manifests can assert current named register values plus decoded channel control, sample, compressed/streamed metadata, loop, pitch, pan/send, balance, volume, key-on fields, playback sample counters, and playback byte counters.
- Add silence-safe AICA register/channel tracking before audible output. Started: AICA MMIO, sound RAM writes, current named register values, decoded sample format/stride/compression metadata, loop state, key-on state, pan/send balance, touched channel snapshots, nominal 44.1 kHz PCM16/PCM8 playback position/loop and loop-end diagnostics, ADPCM packed playback counters, and optional PCM16/PCM8/ADPCM WAV dumps are captured; broader AICA DSP/ARM behavior remains future work.

## Milestone 6: Media And Broader Compatibility

- Parse legal/local media formats. Started: raw 2048-byte data, 2352-byte CD-sector payload extraction, simple CUE data-track selection, and GDI data-track mapping by absolute LBA, including generated multi-track GDI media with nontrivial data-track FADs, non-track-3 data tracks, 2352-byte source sectors, and GDI file offsets.
- Add GD-ROM sector read behavior. Started: firmware GD-ROM HLE can read sectors from the loaded media image into system RAM, KOS `cdrom_read_sectors()` PIO/DMA command layouts are wired to the HLE path, stream commands now return `STREAMING` and support immediate PIO/DMA transfer/check syscalls, queued command execution latches `ASIC_EVT_GD_COMMAND` (`0x0100`), successful DMA reads latch the normal GD-ROM DMA-complete ASIC source `ASIC_EVT_GD_DMA` (`0x000e`), queued command ids now report processing until `GDROM_MAINLOOP` executes them, completed/failed ids become inactive after one reported check, command diagnostics include send/exec/check lifecycle activity with transferred byte counts, generated local media now includes a tiny nested ISO9660 filesystem, and summaries/fixtures record status, sector-mode, TOC, track mapping, and sector/count/destination/status diagnostics for loaded/no-media drive status, reinit sector-mode setup, successful TOC reads, TOC-discovered multi-track/2352-source reads, no-media TOC/read and out-of-range failures, successful raw single-sector, raw multi-sector, missing-file, root directory, nested directory, multi-sector file, and seek/EOF media access.
- Inspect retail-adjacent media without booting it. Started: `media inspect` reports raw/CUE/GDI geometry, CUE track layout, IP.BIN-style Dreamcast boot metadata when a readable boot sector is found, and adjacent CUE-directory boot candidates for odd dump layouts. `media extract-boot` adds a minimal ISO9660 reader and can extract the boot file named by IP.BIN from selected media, a usable adjacent candidate, or a split-track boot extent that lands in a later adjacent track file. `media analyze-boot` compares original and descrambled boot binary layouts, reports startup-stub/opcode heuristics, and can emit a descrambled candidate. `media boot-smoke` maps the selected raw boot binary at `0x8C010000`, seeds the full executable IP.BIN at `0x8C008000` for media inputs, rebuilds CUE directory fallback IP.BIN data from 2048-byte sector payloads, enters the IP.BIN bootstrap with BIOS-like SR/VBlank state, reports key boot-region writes, captures filtered runtime memory writes with `--memory-write-log`/`--memory-write-address`, and can stop on unmapped or selected device-domain accesses to expose concrete emulator gaps.
- Ratchet local retail probes. Started: `tools/probe-retail.ps1` verifies current local-disc checkpoints, including DOA2, Rayman, and Legacy progressing through IP.BIN framebuffer setup, BIOS system function `0` soft-reset handoff, and into their loaded boot binaries without unmapped stops. Legacy also verifies the former `0x330441F0` corrupted-IP.BIN pointer/table blocker stays cleared under a longer local retail budget. Focused traces showed the shared IP.BIN splash timeout path jumps from `0x8C008524` to the BIOS soft-reset vector at `0x8C0000E8`; this is now HLE-modeled as a nonterminal handoff to `0x8C010000` with a high-RAM stack. DOA2's former PVR identity/status wait now clears after modeling `PVR_ID`, `PVR_REVISION`, and `PVR_SYNC_STATUS`; a longer DOA2 run now preserves the low BIOS IRQ handler table, dispatches VBR-zero IRQ9/IRQB through entries such as `0x00000224` and `0x0000022C`, returns callback-style IRQ handlers through a firmware wrapper that restores `SSR/SPC` and the interrupted `PR`, passes the accepted event in banked `r4`, acknowledges accepted ASIC sources, derives the `0x8C0080F0` boot directory from matching ISO9660 media metadata when possible while clearing `0x8C0080FE` on boot handoff, models system function `3` as the nonterminal check-disc helper, reports loaded media as GD-ROM disc type `0x80`, reaches successful DMA reads without re-entering IP.BIN, translates raw CUE filesystem FAD reads through the `trackStart + 150` data-area bias, reports the BIOS GD-ROM send/exec/check lifecycle as processing then completed, and fast-forwards the observed `0x8C114254-0x8C114276` init delay plus loaded-binary scan/clear/no-op callback-timeout loops. The former software busy-bit loop at `0x8C130460` is now guarded and fast-forwarded when `0x8C2F67FC` bit 0 remains set, callback slot 7 is the default no-op, the task-helper queues are empty, and the active queue head matches an observed DOA2 work item in the task arena below `0x8C2F7640`; synthetic completion now also frees the completed task slot so the allocator scan at `0x8C12FD20` does not saturate the arena. A 650M run advanced through later observed busy-bit waits, no-op callback timeouts, and task-slot reuse after 17 successful DMA reads, recording over 103K boot-binary writes and reaching the recurring VBlank/default-event checkpoint around `0x8C12BF42` with pending source `0x0320`; focused higher-budget probes then exposed an FPU overflow/vector-math blocker. FPU traces now annotate non-finite scalar/vector results with raw operands, the runner can emit a bounded instruction-windowed FPU anomaly log for `FR`/`XF` transitions into `NaN` or `Infinity`, trace capture can be narrowed by executed-instruction range for late retail checkpoints, and SH-4 FPSCR arithmetic status records invalid, divide-by-zero, overflow, inexact overflow, and always-inexact `FIPR` cause/sticky flag bits. Finite-input overflow now respects `FPSCR.RM=zero`, so the DOA2 generator at `0x8C1038CC` saturates to `0x7F7FFFFF` instead of `+Infinity`; the former vector helper at `0x8C0F9FAA`/`0x8C0F9FB6` now produces finite `FIPR` values. An 800M post-fix run reaches 27 successful GD-ROM reads, including large asset transfers up to 446 KiB, and stops in the IRQ9/default-event path around `0x8C12BF20`; the no-op callback timeout fast-forward now consumes the synthetic VBlank schedule without latching an extra VBlank event, and the guarded slot-8 rts-stub task callback now raises `0x8C2B6CEC` bit `0x20` at the observed main-poll helper state. The SH-4 decoder also covers GBR displacement loads/stores and byte logical operations after the next frontier exposed `0xC401` at `0x8C0FCD60`. A 150M post-fix run reaches the instruction limit in the `0x8C10076E` floating-point/color packing path with 27 successful GD-ROM reads intact. The recurring `0x8C0FB218` NaN helper still appears in bounded anomaly logs but no longer blocks asset streaming. PC histogram logging is now available through `--pc-profile-log`; the exact DOA2 unrolled word-copy return at `0x8C10E60A-0x8C10E62C`, byte-fill loop at `0x8C10786E-0x8C107878`, trig setup plus recurrence span at `0x8C0FB1C0-0x8C0FB216`, observed not-taken post-trig return tail at `0x8C0FB216-0x8C0FB22C`, single-precision recurrence fallback at `0x8C0FB1FE-0x8C0FB20E`, two-vector scale loop at `0x8C1005A0-0x8C1005B4`, interpolation loop at `0x8C100A7A-0x8C100AB6`, common `0x8C1006EC-0x8C10077C` color-pack path, common `0x8C100AC0-0x8C100BB2` color-byte pack return helper, common `0x8C10077C-0x8C100888` TA emit return helper, common `0x8C0E1E08-0x8C0E1EB0` text glyph setup helper, text advance tail at `0x8C0E1EB2-0x8C0E1EF8`, and the `0x8C0FAC40` trig-argument wrapper are now fast-forwarded when neither trace nor FPU diagnostics are observing them. The TA emit helper preserves P4/store-queue writes through the normal memory API and is additionally guarded against crossing scheduler wake boundaries. The latest 150M profile skips roughly 109.9M CPU instructions, keeps 27 successful GD-ROM reads, removes the text-advance and `0x8C0FAC40` wrapper bodies from the hot profile, and exposes the next cluster in the renderer prologue around `0x8C100430-0x8C1004A0`.
- Latest DOA2 profile-guided update: the renderer prologue common path at `0x8C100430-0x8C100456` is now fast-forwarded after the entry push, including its `fipr` delay slot. The 150M profile now skips roughly 110.5M CPU instructions with 27 successful GD-ROM reads intact, and the next exposed renderer cluster starts at `0x8C1004A0`.
- Latest DOA2 mode-2 renderer update: the exact lookup path at `0x8C1004A0-0x8C1004CC` now fast-forwards when the observed mode-2 branch is selected and the signed index lookup succeeds. The 150M profile now skips roughly 111.2M CPU instructions with 27 successful GD-ROM reads intact, and the next exposed renderer cluster begins at `0x8C100500`.
- Latest DOA2 renderer trig setup update: the exact mode-2 setup bridge at `0x8C100500-0x8C100534` now fast-forwards to the first trig helper call while preserving its FPU math, stack write, delay-slot argument load, and `PR`. The 150M profile now skips roughly 112.0M CPU instructions with 27 successful GD-ROM reads intact, and the next exposed renderer bridge begins at `0x8C100536`.
- Latest DOA2 second-trig bridge update: the exact `0x8C100536-0x8C10053E` call bridge into the `0x8C0FAC40` wrapper and the `0x8C100540-0x8C10055A` post-wrapper bridge into `0x8C100A30` now fast-forward with FPU/trace gates. The 150M profile now skips roughly 112.6M CPU instructions with 27 successful GD-ROM reads intact, and the next exposed renderer body begins at `0x8C10055C`.
- Latest DOA2 renderer mode-word update: the exact `0x8C10055C-0x8C100574` post-call scale setup, observed `0x8C1005B4-0x8C10066E` mode-word setup path, and tiny `0x8C100670-0x8C100672` color-pack return bridge now fast-forward with branch-condition guards for the `mode=2`/`flags=0x2805` path. The 150M profile now skips roughly 115.2M CPU instructions with 27 successful GD-ROM reads intact, and the next exposed renderer body is the `0x8C100A30` call path.
- Latest DOA2 renderer interpolation prologue update: the exact `0x8C100A30-0x8C100A50` call prologue and nested `0x8C10E5CC-0x8C10E5D6` word-copy dispatch now fast-forward to the existing `0x8C10E60A` copy-tail helper under strict opcode/table guards. The 150M profile now skips roughly 115.9M CPU instructions with 27 successful GD-ROM reads intact, and the next exposed renderer body starts at `0x8C100A52`.
- Latest DOA2 renderer interpolation setup update: the observed `0x8C100A52-0x8C100AB4` setup/first-iteration span now fast-forwards through all four interpolation iterations, and the `0x8C100AB6-0x8C100ABC` epilogue return bridge restores `PR`/`fr14` and returns to `0x8C10055C`. The 150M profile now skips roughly 117.4M CPU instructions with 27 successful GD-ROM reads and zero FPU anomalies intact; the remaining renderer entries are mostly fast-forward trigger PCs, and the next newly exposed lower cluster is around `0x8C10751C-0x8C10754C`.
- Latest DOA2 signed remainder update: the exact `0x8C10751C-0x8C1075CC` signed remainder helper now fast-forwards after the entry `tst`, replaying the SH-4 division flag sequence internally instead of replacing it with host arithmetic. The 150M profile now skips roughly 118.0M CPU instructions with 27 successful GD-ROM reads and zero FPU anomalies intact, and the next exposed cluster is the surrounding `0x8C117482` byte classifier plus `0x8C117500` setup/call path.
- Latest DOA2 zero-byte classifier update: the observed `0x8C117482-0x8C1174AE` byte-classifier path now fast-forwards when `[r6+4] == 0` and returns to `0x8C117532`, preserving `r0=0` and `T=1` under exact opcode/constant guards. The 150M profile now skips roughly 118.1M CPU instructions with 27 successful GD-ROM reads and zero FPU anomalies intact, and the next exposed cluster is the surrounding `0x8C117500-0x8C117528` setup/call span.
- Latest DOA2 list-entry setup update: the exact `0x8C11750E-0x8C117530` setup span now fast-forwards to the byte-classifier entry after preserving the table-derived stores, `MACL`, stack push, delay-slot argument load, and `PR`; the `0x8C117532-0x8C11753C` post-classifier dispatch also fast-forwards to the signed-remainder helper. The 150M profile now skips roughly 118.2M CPU instructions with 27 successful GD-ROM reads and zero FPU anomalies intact, and the next exposed cluster is the allocator calls at `0x8C124634`/`0x8C124646` plus the later `0x8C11753E` branch path.
- Latest DOA2 list-entry allocator update: the exact `0x8C117500-0x8C11750C` allocator-pair bridge now fast-forwards through `0x8C124634`, `0x8C124646`, and the nested `0x8C134F48` base-plus-stride helper while preserving returned pointers, `MACL`, `T`, stack push/pop, and `PR`. The 150M profile now skips roughly 118.4M CPU instructions with 27 successful GD-ROM reads and zero FPU anomalies intact, and the next exposed local cluster is the `0x8C11753E`/`0x8C117540` branch plus list-tail updates at `0x8C11759E-0x8C1175A8`.
- Latest DOA2 nonzero list-tail update: the exact `0x8C11753E-0x8C117540` nonzero-remainder branch now fast-forwards through `0x8C11759E-0x8C1175A8`, storing `r12 + 20`, incrementing `r13`, preserving the signed loop compare, and landing on the next iteration or exit. The 150M profile now skips roughly 118.5M CPU instructions with 27 successful GD-ROM reads and zero FPU anomalies intact, and the next newly exposed cluster is around `0x8C0F7D2A-0x8C0F7D58`.
- Latest DOA2 zero-status scan update: the exact `0x8C0F7D2A-0x8C0F7D58` zero-entry table scan now fast-forwards across the known status table after an already-read zero word, updating `0x8C0F7EE4` and landing on the first nonzero entry compare or table-end return path. The 150M profile now skips roughly 118.5M CPU instructions with 27 successful GD-ROM reads and zero FPU anomalies intact, and the next newly exposed cluster is around `0x8C103AF0-0x8C103B0C` plus `0x8C10E5D8-0x8C10E5E0`.
- Latest DOA2 scratch-vector wrapper update: the exact `0x8C103AF0-0x8C103B0C` staging wrapper now fast-forwards after its entry literal load, including the nested `0x8C10E5D8` dispatch and `0x8C10E61E-0x8C10E62A` copy tail. The 150M profile now skips roughly 118.6M CPU instructions with 27 successful GD-ROM reads and zero FPU anomalies intact; the nested copy body is gone from the hot profile, `0x8C103AF0` remains as the trigger instruction, and the next newly exposed lower cluster is around `0x8C0F29DE-0x8C0F29F2`.
- Latest DOA2 table-entry address update: the exact `0x8C0F29DE-0x8C0F29F2` address helper now fast-forwards after its entry `mov r4,r0`, preserving wrapping arithmetic and the final `T` bit from the last shift while returning `0x8C2AD770 + index * 96`. The 150M profile now skips 118,592,296 CPU instructions with 27 successful GD-ROM reads and zero FPU anomalies intact; only `0x8C0F29DE` remains as the trigger PC, and the next exposed smaller clusters are around `0x8C107878` and `0x8C013BF6-0x8C013C34`.
- Latest DOA2 zero-record group scan update: the exact low-memory `0x8C013BF6-0x8C013CE2` scan now fast-forwards from the first unrolled group test when the remaining 8-byte records through index 31 have zero first words, restoring the saved registers and returning through the normal epilogue. The 150M profile now skips 118,682,536 CPU instructions with 27 successful GD-ROM reads and zero FPU anomalies intact; the `0x8C013Cxx` body is gone from the hot profile, and the next exposed smaller clusters are around `0x8C0F3678-0x8C0F36B0` and `0x8C107424-0x8C10742E`.
- Latest DOA2 zero-status byte table scan update: the exact `0x8C0F3678-0x8C0F36CA` wrapper loop now fast-forwards across remaining zero status bytes in the 96-byte table, including nested address-helper effects, `0x8C2ADA94` progress update, and normal epilogue restoration. The 150M profile now skips 118,702,536 CPU instructions with 27 successful GD-ROM reads and zero FPU anomalies intact; the `0x8C0F36xx` body is gone from the hot profile, and the next exposed smaller cluster is the `0x8C107424` division helper.
- Latest DOA2 unsigned divide update: the exact nonzero-divisor `0x8C107424-0x8C1074B4` helper now fast-forwards after its entry `tst`, replaying the SH-4 `div0u`/`rotcl`/`div1` state machine and restoring `r2` through the normal return path. The 150M profile now skips 118,853,952 CPU instructions with 27 successful GD-ROM reads and zero FPU anomalies intact; the division body is gone from the hot profile, and the next exposed smaller cluster is the surrounding byte-fill wrapper/return path at `0x8C107864-0x8C10787A` and `0x8C11C7C0`.
- Latest DOA2 byte-fill wrapper update: the exact `0x8C107864-0x8C10787A` byte-fill helper now fast-forwards from entry through the return delay slot, preserving the filled byte range plus `r0=r4`, `r7=r6`, and final `T=true`. The 150M profile now skips 118,875,512 CPU instructions with 27 successful GD-ROM reads and zero FPU anomalies intact; only `0x8C107864` remains as the trigger PC, and the next exposed cluster is the caller loop around `0x8C11C7C0-0x8C11C7E0`.
- Latest DOA2 table divide setup update: the exact `0x8C11C7B0-0x8C11C7E8` caller now fast-forwards the eight-word setup loop around the unsigned divide helper, preserving the helper stack write plus final quotient and `M/Q/T` state. The 150M profile now skips 118,923,275 CPU instructions with 27 successful GD-ROM reads and zero FPU anomalies intact; the loop body and `0x8C107424` trigger are gone from the hot profile, and the next exposed cluster is the post-loop/callback path around `0x8C11C7EE-0x8C11C802` and `0x8C12FDA8-0x8C12FDC2`.
- Latest DOA2 post-table vector copy update: the exact `0x8C11C7EA-0x8C11C802` loop now fast-forwards the eight-word copy from `r13+0x8F4` to `r14+36` through `r14+64`, preserving the final registers and `T=true`. The 150M profile now skips 118,946,504 CPU instructions with 27 successful GD-ROM reads and zero FPU anomalies intact; the copy body is gone from the hot profile, and the next exposed cluster is the callback-table stride scan at `0x8C12FDA8-0x8C12FDC4`.
- Latest DOA2 empty callback-table scan update: the exact `0x8C12FDA0-0x8C12FDCA` empty path now fast-forwards only when all eight callback table heads at `0x8C2F6820 + n*0x1C4` are not active, preserving the zero return and terminal counter state. The 150M profile now skips 118,964,774 CPU instructions with 27 successful GD-ROM reads and zero FPU anomalies intact; the scan body is gone from the hot profile, and the next exposed cluster is around `0x8C11B226-0x8C11B33E`, `0x8C0F90B8-0x8C0F90C2`, and `0x8C1306BC`.
- Latest DOA2 five-word table copy update: the exact `0x8C11B226-0x8C11B254` loop now fast-forwards the fixed 20-byte selected-table copy, preserving the final registers and `T=true`. The 150M profile now skips 118,995,833 CPU instructions with 27 successful GD-ROM reads and zero FPU anomalies intact; the copy loop is gone from the hot profile, and the next exposed clusters are `0x8C11B300-0x8C11B33E`, `0x8C0F90B8-0x8C0F90C2`, and `0x8C1306A0`.
- Latest DOA2 five-word mirror copy update: the exact `0x8C0F90B0-0x8C0F90C6` helper loop now fast-forwards the fixed five-word mirror copy into `0x8C2B6F68`, preserving the terminal pointers and `T=true`. The 150M profile now skips 119,003,402 CPU instructions with 27 successful GD-ROM reads and zero FPU anomalies intact; the mirror copy is gone from the hot profile, and the next exposed clusters are `0x8C11B300-0x8C11B33E` and `0x8C1306A0-0x8C1306BC`.
- Latest DOA2 empty stack word scan update: the exact `0x8C11B300-0x8C11B33E` empty path now fast-forwards the zero-word scan from `r7` to `r13`, preserving terminal pointer/counter state and landing at `0x8C11B340`. The 150M profile now skips 119,014,103 CPU instructions with 27 successful GD-ROM reads and zero FPU anomalies intact; the stack scan body is gone from the hot profile, and the next exposed cluster is `0x8C1306A0-0x8C1306D4`.
- Latest DOA2 empty task-helper return update: the exact `0x8C1306A0-0x8C13073A` zero-table path now fast-forwards after the entry push when all observed table reads are zero, preserving stack writes/restores and the normal return through `PR`. The 150M profile now skips 119,057,096 CPU instructions with 27 successful GD-ROM reads and zero FPU anomalies intact; the helper body is gone from the hot profile, and the next exposed cluster is `0x8C1307D6-0x8C1307F4`.
- Latest DOA2 empty task-helper caller-loop update: the exact `0x8C1307D6-0x8C1309BA` empty caller loop now fast-forwards remaining empty entries after the already-executed literal load, preserving stack writes, helper-return effects, `PR`, and terminal counter state. The 150M profile now skips 119,091,281 CPU instructions with 27 successful GD-ROM reads and zero FPU anomalies intact; the caller-loop body is gone from the hot profile, and the next exposed dense clusters are around `0x8C0FB1C0`, `0x8C0E1E08`, and `0x8C100670`.
- Latest DOA2 renderer mode-2 entry aggregate update: the exact `0x8C100430-0x8C100534` entry span now fast-forwards through prologue, lookup, and first trig-call setup under combined opcode/table/FPSCR guards. The 150M profile now skips 119,155,961 CPU instructions with 27 successful GD-ROM reads and zero FPU anomalies intact; `0x8C1004A0` and `0x8C100500` are gone from the hot profile, and the next renderer aggregate target is around `0x8C100536-0x8C10055C`.
- Latest DOA2 AICA mailbox timeout update: the hot `0x8C104D4C-0x8C104D56` loop is a zero-mailbox timeout over uncached AICA sound RAM at `0xA080005C`, not a PVR register poll. It now fast-forwards the observed zero path under exact opcode/literal/register guards, partial scheduler-wake clamping, and memory-read-watch gating. The 150M profile still reaches `PC=0x8C10076E` with 27 successful GD-ROM reads and the same `nonfinite:32397` sprite source group, but CPU fast-forward rises to 144,321,023 instructions and the old six-instruction loop disappears from the hot profile. A 600M no-trace run remains behaviorally unchanged at 27 GD-ROM reads and `taSpritePreview=renderable:0/degenerate:0/nonfinite:506026`, confirming this slice is reach/performance plumbing rather than the final renderability fix.
- Latest DOA2 high-RAM/cache maintenance update: the `0x8C11333C-0x8C113344` loop is an exact high-RAM byte-zero fill from the table pointers at `0x8C14D4D4/0x8C14D4E0`, and the `0x8C111ED8-0x8C111EDE` loop is an `ocbp @r0` cache-block purge over 32-byte steps. Both now fast-forward with strict opcode/state guards and scheduler-wake clamping; the byte-fill path is disabled when memory read/write watches are active. The 150M DOA2 profile still reaches `PC=0x8C10076E` with 27 successful GD-ROM reads and the same all-nonfinite sprite source group, while CPU fast-forward rises to 145,266,039 instructions and both clusters disappear from the hot profile. The next exposed top cluster is the BIOS/IP.BIN-ish `0x8C00909A-0x8C0090A0` group, followed by the known qNaN renderer/trig paths.
- Latest IP.BIN ASIC-event wait update: the `0x8C00909A-0x8C0090A0` loop writes/polls ASIC event register A at `0xA05F6900`, waiting for bit 3 to be raised by the next VBlank event after acknowledging it. The fast-forward now skips only repeated zero polls, is trace- and memory-read-watch gated, and uses a VBlank-event-aware scheduler clamp rather than requiring VBlank IRQ enablement. The 150M DOA2 profile still reaches `PC=0x8C10076E` with 27 successful GD-ROM reads and the same all-nonfinite sprite source group, while CPU fast-forward rises to 145,565,295 instructions and the ASIC wait disappears from the hot profile. The next top entries are the known renderer/trig qNaN frontier around `0x8C0FB1C0`, `0x8C1038C6`, and `0x8C100430`.
- Latest SH-4 FPU `...D` decode update: the qNaN renderer frontier was an opcode-family bug. `F79D`, `F58D`, and `F69D` are now decoded as `fldi1`, `fldi0`, and `fldi1`, while `fipr` is restricted to the SH-4 graphics opcode form `1111nnmm11101101` (`0xF?ED`). The DOA2 trig and interpolation fast-forwards were updated to mirror the corrected instruction stream. A 93M DOA2 probe now reports `taSpritePreview=renderable:20/degenerate:0/nonfinite:0`; the 150M sprite probe reaches `PC=0x8C1007EE`, keeps 27 successful GD-ROM reads, and reports `taSprites=32901` from the same renderer source PCs. Later raw-float preview coordinates classify the full 150M set as `taSpritePreview=renderable:32901/degenerate:0/nonfinite:0`, and `tools/probe-retail.ps1 -Doa2SpriteProbe` locks that source group instead of the old nonfinite group.
- Latest DOA2 thin-sprite preview update: the PVR sprite preview path now preserves raw subpixel coordinates for renderability and paints a conservative footprint for subpixel-thin quads that span pixel columns or rows. The 150M DOA2 preview reports `taSprites=32901`, `taSpritePreview=renderable:32901/degenerate:0/nonfinite:0`, and the framebuffer snapshot advances from sparse all-degenerate output to nonzero preview pixels when treated as a flat sprite/UI stream.
- Latest DOA2 TA emit/provenance update: watched memory accesses now retain the producer opcode and decode common SH-4 write source or read target registers in memory logs. A focused `0x8C100780-0x8C100850` trace shows the final sprite SQ packet reading X coordinates from the `r11` source buffer at `0x8C2B6BC0+`, Y coordinates from the `r10` source buffer at `0x8C2B6BB0+`, and stamping the same `fr13` Z value into all three Z lanes before the `0x8C10084C-0x8C100850` `pref` flush. Sprite command texture payloads now contribute to effective sprite texture enablement; with that honored, the same 150M stream groups as effective twiddled Argb1555 texture sprites and the preview records 164,737 texture-sampled pixels, 164,215 of them zero-alpha, leaving `fbNonZero=0`. The current frontier is therefore texture source/pixel-format/upload interpretation, not an immediate nonfinite coordinate producer.
- Superseded DOA2 nonfinite sprite provenance note: repeated `--fpu-write-register` / `--fpu-memory-register` filters closed the renderer-entry trace gap and previously showed qNaN coordinates flowing from the helper at `0x8C0FB1C0-0x8C0FB22C` into the renderer stack/source buffers. That trace was useful, but the later SH-4 `...D` decode fix showed the root cause was not guest object initialization: the helper's `F79D`/`F58D`/`F69D` scalar immediate loads had been decoded as `fipr`, clobbering constants and forcing the bogus `0/0` path. The zero argument from `[0x8C2B6C00+0x28]` remains a real guest value, but the corrected helper no longer turns it into qNaN sprite geometry.
- Latest SH-4 `FTRC` accuracy update: single/double `ftrc` no longer relies on host casts. It now truncates toward zero, saturates positive/negative out-of-range results to signed 32-bit limits, maps NaN input to `0x80000000`, and records invalid cause/sticky bits for NaN or saturated conversion. This matters for the DOA2 trig/helper path because qNaN can flow through `ftrc fr3,fpul` at `0x8C0FB1EC`; the focused 150M probe now shows that qNaN becoming `fpul=0x80000000`, `sts fpul,r6`, and `float fpul,fr3` producing `0xCF000000`. The run still reaches the same qNaN TA sprite payload frontier, so `FTRC` accuracy is improved but is not the final renderability blocker.
- Superseded PVR sprite renderability reporting note: run summaries and CLI/fixture text include aggregate TA sprite preview counts (`renderable`, `degenerate`, `nonfinite`) instead of only the recent sprite tail. This originally exposed the all-nonfinite DOA2 frontier, but the current finite checkpoint is the SH-4 `...D` decode update above.
- Superseded TA sprite source-PC note: assembled sprite summaries retain the header, control, and payload instruction PCs from their underlying TA writes, structured video summaries expose `pvrTaSpriteSourceGroups`, fixture manifests can assert those groups, and `tools/probe-retail.ps1 -Doa2SpriteProbe` locks the current 150M JSON checkpoint. The same source-PC machinery now asserts the single `renderable:32901` DOA2 sprite group instead of the old all-qNaN group.
- Add retail FPU provenance tooling. Started: the runner can emit a bounded `--fpu-write-log` for selected `FR`/`XF` register writes with optional instruction-window and register filters, a bounded decoded `--fpscr-log` for FPSCR changes/accesses that does not perturb unrelated fast-forwards, a bounded pre-instruction `--fpu-snapshot-log` for full `FR`/`XF` snapshots at selected PCs, a bounded `--fpu-memory-log` for `fmov.s`/`fmov.d` load/store transfers with register/address filters, and CLI trace logs include executed-instruction numbers so traces correlate cleanly with FPU/memory/device logs. The DOA2 write-log lead showed `fr6` overflowed in `0x8C1038CC`; after modeling `FPSCR.RM=zero` result selection, those writes now become `0x7F7FFFFF` with overflow/inexact status instead of `0x7F800000`, the helper consumes a finite lane, and the focused 90M probe records no FPU anomalies. The next FPU tooling need is not another immediate trap probe, but longer post-fix retail frontier capture if a new non-finite path appears.
- Run public redistributable demos and user-provided local images through the artifact pipeline.
- Latest Sonic intro update: `PVR_SYNC_STATUS` exposes bit `0x2000` during the synthetic VBlank status window, the default `0x8C000000` firmware callback returns to `PR` with `r0=0`, and queued GD-ROM commands snapshot their first four parameter words at send time. Sonic Adventure now reaches 8 successful reads before BIOS menu function `1`, Sonic Adventure 2 reaches the 50M budget with 45 successful reads, and Sonic Shuffle reaches a successful read plus nonzero framebuffer bytes before a boot-area branch to `0x8C008300`; see `docs/sonic-intro-milestone.md` and `tools/probe-sonic.ps1`.
- Latest Sonic Adventure 2 G2/AICA update: G2 DMA channel 0 now handles the AICA DMA register block, performs system-RAM/AICA-RAM copies, clears the start bit, and raises ASIC event `0x000f`. SA2 also has exact, trace-gated fast-forwards for its VRAM clear, post-load system-RAM word/byte clears at `0x8C10052A` and `0x8C100554`, AICA PIO read-word helper, external/modem G2 read probes, the `0x8C1543A0` AICA word-read wrapper, the `0x8C16BF10` AICA byte-read adapter, AICA status wait, AICA PIO write loop, AICA work-queue no-work polling, the IRQ-side `0x8C153A90` AICA work-poll wrapper, the outer `0x8C12F7F2-0x8C12F802` AICA work-poll loop, G2 DMA status clear/set helpers, the AICA-driver `EXEC` completion wait, the IP.BIN zero-bit glyph walker around `0x8C0089F6-0x8C008A74`, the empty pointer-table scan around `0x8C135CD8-0x8C135D06`, the `0x8C10F440-0x8C10F446` cache-invalidate loop, the `0x8C134F3E-0x8C134F76` record-hash scan, the AICA no-work slot scan/cleanup pair around `0x8C15B604-0x8C15B690` and `0x8C15B83C-0x8C15B860`, the AICA name-call bridge and loop tail around `0x8C15B91A-0x8C15B936`, the AICA channel setup bridge at `0x8C15C4DE-0x8C15C564`, the AICA post-setup flag tail at `0x8C15C57E-0x8C15C5AA`, the AICA descriptor-copy helper at `0x8C15C622-0x8C15C680`, the inactive AICA channel tail at `0x8C15C780-0x8C15C856`, the AICA channel flag return tail at `0x8C15C5AC-0x8C15C5DA`, and the `0x8C023E20` PRS-style asset decompressor. The current 120M probe reaches 47 GD-ROM reads, skips `116,549,088` CPU instructions, and has shifted the top profile away from the `0x8C10F440` cache loop, `0x8C134F3E` record-hash scan, descriptor-copy body, post-setup flag-tail body, and AICA name loop body; the remaining top entries are IP.BIN set-bit/draw paths plus the SA2 helper around `0x8C134CA8`, with `0x8C15B918`, `0x8C15B92E`, and `0x8C15C57C` still visible as trigger PCs and no PVR TA writes yet. A 500M post-PRS run reaches 47 GD-ROM reads, including the 882,688-byte asset DMA to `0x0C428800` and follow-up 61,440-byte DMA to `0x0C300000`, still with no PVR TA writes, and advances past the former active `0x8C16B58E` work-helper wall to `PC=0x8C15BF8C`. The next Sonic task remains replacing the remaining AICA work-structure/mailbox skips with minimal ARM/audio-driver completion semantics so the game can progress far enough to emit TA traffic.
- Latest broad retail sweep: the 2026-06-05 pass over 11 local retail titles is captured in `docs/retail-compatibility-sweep-2026-06-05.md`. Seven titles reach the generic 50M instruction budget after the GDI extraction and absent-device passes. The remaining frontiers were Sega Rally 2's WinCE scheduler/list-management path, Sonic Adventure/Sonic Adventure 2 zero-PC firmware/callback state, and The Grinch's BIOS CD-menu exit; the Sonic zero-PC callback state is superseded by the focused Sonic intro update above. The active near-term retail milestone now uses Sonic Adventure, Sonic Adventure 2, and Sonic Shuffle as focused canaries for getting intro sequences running.
- Latest GDI extraction update: ISO9660 probing now prefers later data-track volumes, matching retail GDI images that contain a small low-density session ISO plus the real high-density game ISO. Gauntlet Legends, The Grinch, Sonic Adventure, Sonic Shuffle, and Wetrix+ now extract `1ST_READ.BIN` directly from GDI; the post-fix GDI sweep moves Gauntlet, Sonic Shuffle, and Wetrix+ to the 50M instruction budget, keeps The Grinch on BIOS CD-menu function `2`, and exposes a Sonic Adventure zero-PC firmware/callback frontier that differs from its CUE fallback.
- Latest absent-device mapping update: the first device-frontier pass classifies and models the modem window at `0x00600000-0x006007FF`, area-0 external-device windows including the `0x02000000-0x03FFFFFF` lower-area mirror, and the AICA RTC window at `0x00710000-0x0071000B`. Crazy Taxi now moves past `0xA30100C0` to the 50M instruction budget in a cached-bootstrap SCIF wait, and the Sonic Shuffle CUE fallback moves past `0xA0600004` plus `0xA0710000` to a zero-PC firmware/callback frontier instead of an unmapped read.
- Latest WinCE bootstrap update: `media analyze-boot` now detects Sega Rally 2's original-layout `0WINCEOS.BIN` header, reports the header entry offset `0x800`, and applies the WinCE GD-ROM payload rule that skips the first `0x800` bytes before loading the remaining image at `0x8C010000`. `boot-smoke` now seeds the BIOS soft-reset handoff with the corrected selected entry, so Sega Rally 2 reaches the entry stub at `0x8C010000`, jumps to executable code at `0x8C0120E0` / file offset `0x28E0`, executes `ldtlb`, installs a page-size-aware low virtual RAM page, and passes the observed banked-register `ldc`. That retired the previous image-relocation diagnosis and exposed the WinCE API/syscall thunk family.
- Latest WinCE syscall/MMU update: the first Sega Rally 2 odd negative thunk (`0xFFFFFD1F`) decodes as `WIN32.PerformCallBack`. The firmware trap hook now recognizes WinCE API-call addresses before instruction fetch, decodes useful names, dispatches `PerformCallBack` through the observed `CALLBACKINFO` block (`hProc`, `pfn`, `pvArg0`), preserves and logs the callback context registers (`r5/r6/r7`), returns simple unknown WinCE calls to `PR` with `r0=0`, models `CURPROC.SetProcPermissions` as a stateful permission-mask swap, and gives `WIN32.CreateCrit` stable nonzero backing handles per critical-section address. The callback leads into a `coredll.dll` module/API lookup path, then the loader builds a low virtual section table at `0x02010000`. MMU-enabled low virtual memory is now backed separately from the no-MMU area-0 external mirror, and WinCE section mappings can fall back to the loaded source descriptors, letting Sega Rally execute the mapped module entrypoint at `0x01E324DC` instead of decoding zero. The scheduler log now follows the current-thread, module/list-root, and captured nested object pointers through WinCE object pages, including named key fields even when those fields are zero. Probes confirm the module object at `0x8CEEE5F4` is live and records the mapped entrypoint, while all timer-wheel slots remain empty. The post-entrypoint caller reads current-thread `+0x48`, which is copied from nested source object `0x8C1376C0 + 0x1C`; a focused 9.1M trace shows the broad `0x8C02EBCA` zero is arena clearing and `0x8C01DD76` is the structured initializer deliberately storing `r2=0` to that source field. `--memory-write-changed-only` and `--memory-write-distinct` are now available for broad watched-write probes. The compact Sega Rally structural watch shows normal module/thread setup followed by a coherent slot-1 enqueue/removal pair, then a stable dispatch loop where the wait-active flag toggles, `current-wait-delta=7`, all timer-wheel slots stay empty, and `scheduler-pending-tick-delta` briefly becomes `0x19` before being cleared. A focused `0x00005BC0`/`0x8C017790-0x8C0178B4` trace confirms that value is the returned `kernel-tick-delta` from `0x8C13188C`, not a runqueue pointer. The empty-list consumer is now decoded: a synthetic entry at `scheduler-expired-list-head` (`0x8C1365AC`) shows the branch subtracts pending ticks from entry `+0x28`, removes mature entries through `+0x2C`, clears flag bit `0x0800`, and requeues callback-less entries through the timer-wheel helper at `0x8C016584`. Exact write watches show `0x8C1365AC` is only touched by the `0x8C02EBDA` zero-fill in the 12.5M window. The live `0x8C01AAA8 -> 0x8C01633C` path is a per-object priority-bucket insertion: it queues `0x8C137554` under `0x8CEEE448` at bucket/priority `1`, marks the current thread with `0x0200`, clears `0x8C136544`, and returns `0x102` without producing an expired-list entry. A 50M follow-up records no `0x8C01B160-0x8C01B240` hits and no nonzero expired-list writes; the scheduler remains in the same loop with `scheduler-wait-active-flag=1`, all timer-wheel slots empty, and `scheduler-expired-list-head=0`. Memory-read watches deliberately disable the IP.BIN ASIC wait fast-forward, so read-heavy probes can make `0x8C00909A` look hot even though no-watch profiles collapse it. The next frontier is a missing wake/runnable source or the caller that should leave the current-thread object runnable, not a likely null-slot HLE implementation, queue insertion bug, runqueue-head set/clear bug, or the unhit `0x8C01B160` literal user.
- Latest Sega Rally WinCE branch probe: changed-only current-thread writes confirm the guest deliberately transitions the current thread flags `0x0011 -> 0x0111 -> 0x0011 -> 0x0211`; the timer-wheel helper adds `0x0100`, scheduler removal clears it, and the `0x8C01AAA8 -> 0x8C01633C` priority-bucket path adds `0x0200`. The mapped module caller at `0x01E324DC` writes the module object to `0x01E4C0E8`, calls `CURPROC.SetProcPermissions` (`0xFFFFF9F9`) with `r4=0x42`, and now takes the nonzero old-permission branch. That branch initializes three critical-section objects through `WIN32.CreateCrit` (`0xFFFFFD65`), storing stable HLE handles at object `+0x8`, then still returns success (`r0=1`) to `0x8C020660`; the kernel-side parent tests `current-thread +0x48` at `0x8CEEEEE4`, sees zero, and exits toward the same scheduler path. A 20M post-HLE probe records the five expected WinCE thunks, no GD-ROM reads, no TA work, `0x8C131AA4/AA8=0`, and the same empty timer-wheel/scheduler loop. The new one-shot `--memory-poke-pc` diagnostic decodes the hidden constructor branch without code hacks: forcing source `0x8C1376C0 +0x1C` nonzero at `0x8C017B7E` calls `0x8C017AC4`, allocates `0x8CEEEE08`, stores it into `current-thread +0x48`, and zeroes object offsets `+0x00`, `+0x08`, and `+0x14`. The following parent then passes the null test but rejects the descriptor because `source +0x0C` is `0x02000000` while `source +0x20` is `0x8C011924`; both values are shifted by 25 and compared as region ids (`1` vs `0x46`). A no-poke producer trace confirms `source +0x0C` is guest-computed at `0x8C01DD56-0x8C01DD58` as `(source byte 0 + 1) << 25`, `source +0x20` is the literal `0x8C011924`, `source +0x38` is then computed as `0x02000000 + 0x8C010000 = 0x8E010000`, and `source +0x1C` is still hard-zeroed at `0x8C01DD76`. The WinCE scheduler log now emits those descriptor summaries directly, suppresses `0x00C0C0C0` fill lookalikes, and reports the active object chain rooted at `0x8C1364B8`. A 20M compact probe shows that active chain is a single coherent node (`0x8CEEE6FC -> null`, owner/link `0x8C135A40`) rather than a runaway list, while the descriptor copy-source remains null, `0x8C131AA4/AA8` remain zero, and all timer-wheel slots remain empty. A second synthetic poke that also changes `source +0x0C` to `0x8C010000` reaches a later scheduler/tick loop around `0x8C0123AE`, `0x8C0178EC`, and `0x8C02DB24`, advances `kernel-tick-total`/`scheduler-tail-state`, and exposes transient dispatch/list fields at `0x8C131AA0` and `0x8C131B20`, but still records no GD-ROM reads or TA work. A focused producer trace shows `0x8C131AA0=0x8C012296` is installed once during cached kernel table setup at `0xAC013456`, while the hot scheduler loop reads `0x8C131AA4` and `0x8C131AA8` as zero; `0x8C131B20=0x8C136410` is part of the larger `0x8C131B00` module/region record initialized at `0x8C0295D0-0x8C0295F2`. A real no-poke consumer trace corrects the module-root picture: `0x8C022F4C` writes `0x8C131B24=0x8CEEE5F4`, and `0x8C01E5FA` walks that live module object, including mapped entrypoint metadata. The synthetic region-matched path leaves `0x8C131B24` absent because it builds a different object whose `+0xB0` is zero; the real path has `+0xB0=0x02010000`, branches through `0x8C022F20`, and links the module root. The next target is therefore the producer or state transition that should make `0x8C131AA4/AA8` nonzero, not the setup-only `0x8C131AA0` vector entry, the now-modeled WinCE permission/critical-section thunks, the bounded active-chain constructor, or the synthetic-poke module-root side effect.
- Latest Sega Rally dispatch-table probe: the `0x8C0123C2-0x8C012456` post-scheduler return path is now decoded. When the scheduler returns the current thread in `r0`, the code saves KData current-thread/source fields, computes `source +0x0C >> 23`, indexes the `0x8C1318A0` dispatch table, stores the selected entry into `0x8C1318A0`, then restores the thread context and `rte`s through `SPC=0x8C018C90`. The later idle branch returns zero from the scheduler, calls the short `0x8C0120FC` helper, and loops back to `0x8C0123AE`. The compact WinCE scheduler log now includes dispatch-table slots plus byte-level flags for `0x8C131884..887`; a 20M no-watch probe records slots 0/1 as `0x8C135B40`, slots 2/3 as the default `0x8C012B2C`, the active chain still as `0x8CEEE6FC -> null`, `0x8C131AA4/AA8` still zero, and all timer-wheel slots empty. A focused restored-thread trace shows `0x8C131885` starts at zero before `0x8C018C90`, then later gates the `0x8C0127B6` wrapper after being set to one; word stores to `0x8C131884` can clear both the yield-request and critical bytes. The `0x8C01AAA8` path sets current-thread flags to `0x0211`, stores the per-object bucket pointer at current-thread `+0x34`, sets the yield byte, returns `0x102`, and the wrapper saves that return plus SP/PR/SR into current-thread context fields `+0x70/+0xAC/+0xB0/+0xB4`. A narrow re-entry trace shows the wrapper then branches directly through `0x8C012398`, clears the volatile saved-register area, zeroes `0x8C131884` at `0x8C0123C0`, scans empty `next-wait-delta`/timer-wheel state, returns `r0=0`, and falls into the idle branch without resuming saved PR `0x8C02647A`. The scheduler log now summarizes the shaped `0x8C137554` work item and resolved object `0x8CEEE448`; the item is present in that bucket, and a read/write watch shows `0x8C136544` is transiently produced by the timer-wheel scan then cleared by `0x8C01AB1E` after the bucket insert. `--memory-read-distinct` is now available for compact consumer probes. A 50M distinct-read bucket sweep shows no read of queued `0x8CEEE450=0x8C137554` after the `0x8C016398` insert, while a dispatch-ring follow-up shows the hot scheduler loop repeatedly reads ring head/tail `0x8C131AA4/0x8C131AA8` as zero at `0x8C0176F2/0x8C0176F4` and no guest write makes them nonzero. Synthetic ring probes decode `0x8C131AAC + head` as a priority byte, `0x8C1319A0 + priority*4` as a mask word, and `0x8C131B40` as the selected queue head checked by the final low helper; forcing those fields still falls back to idle when that selected queue is empty. An 80M producer sweep with exact write watches over the ring, priority masks, selected queue, bucket, and work item records no real writes to ring head/tail, ring priority bytes, or priority masks; only setup writes hit `0x8C131AA0` and `0x8C131B44`. The scheduler log now prints one-line dispatch-ring and yield-handoff summaries so future probes show head/tail equality, priority bytes, masks, selected queue state, current-thread flags, work item, resolved bucket object, and saved wrapper context together. A 12M handoff run records `yield-handoff-summary ... current-thread=0x8CEEEE9C thread-flags=0x00000211 work-item=0x8C137554 ... saved-pr=0x8C02647A`, while a `0x8C012730-0x8C012780` trace shows the recurring `0x8C012768` path only ORs the yield byte back in and `rte`s to the interrupted scheduler PC. The narrowed target is still the missing activation/consumer path from the priority bucket into runnable scheduler state, especially why `current-thread +0x48` remains zero and no producer ever feeds the dispatch ring/masks.
